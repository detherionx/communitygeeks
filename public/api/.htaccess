# Contact endpoint folder: only contact.php is reachable; the configuration
# (and its example) are never served.
<FilesMatch "^contact\.config(\.example)?\.php$">
  Require all denied
</FilesMatch>
Options -Indexes
