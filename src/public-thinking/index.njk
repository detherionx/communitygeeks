---
layout: layouts/base.njk
lang: en
title: Public Thinking
description: Research findings, executive one-pagers, case studies, and field notes — live, not a static portfolio.
permalink: /public-thinking/
extraHead:
  - "<link rel='alternate' hreflang='en' href='https://communitygeeks.de/public-thinking/'><link rel='alternate' hreflang='de' href='https://communitygeeks.de/de/public-thinking/'><link rel='alternate' hreflang='x-default' href='https://communitygeeks.de/public-thinking/'>"
extraScripts:
  - "public-thinking-filter.js"
closingCta:
  secondaryLabel: "See how we work →"
  secondaryUrl: "/approach/"
---
<section class="page-hero">
  <div class="wrap">
    <div class="page-hero-row">
      <div class="page-eyebrow">
        {% include "partials/node-mark.njk" %}
        Public Thinking
      </div>
      <div class="pt-lang-switch">
        <span class="active">EN</span> <a href="/de/public-thinking/">DE</a>
      </div>
    </div>
    <h1 class="page-title">Live findings, not a static portfolio.</h1>
    <p class="page-intro">Research findings, executive one-pagers, case studies, and field notes, in whatever format the finding actually calls for. Each piece is tagged by format and by how much evidence currently supports it.</p>
  </div>
</section>

<div class="wrap">
  <div class="filter-bar">
    <button class="filter-pill active" data-filter="all">All</button>
    <button class="filter-pill" data-filter="research">Research Findings</button>
    <button class="filter-pill" data-filter="onepager">Executive One-Pagers</button>
    <button class="filter-pill" data-filter="case">Case Studies</button>
    <button class="filter-pill" data-filter="field">Field Notes</button>
  </div>

  <ul class="pt-index-list">
    {% for piece in publicThinking %}
    <li class="pt-index-row" data-type="{{ piece.filterType }}">
      <a href="{{ piece.url }}">
        <span class="pt-index-title">{{ piece.title }}</span>
        {% if piece.deck %}<span class="pt-index-deck">{{ piece.deck }}</span>{% endif %}
        <span class="pt-index-meta">
          <span>{{ piece.format }}</span>
          <span>{{ piece.date | readableDate }}</span>
          <span class="confidence-tag">{{ piece.confidence }}</span>
        </span>
      </a>
    </li>
    {% endfor %}
  </ul>

  <div class="pt-index-empty" data-type="research">More research findings are in progress and will publish here as they clear review.</div>

  {% if clients.selectedWork and clients.selectedWork.length %}
  <div class="pt-index-empty" data-type="case">Case studies are experience references for now — full write-ups are in progress: {% for client in clients.selectedWork %}{{ client.name }}{% if not loop.last %}, {% endif %}{% endfor %}.</div>
  {% endif %}
</div>
