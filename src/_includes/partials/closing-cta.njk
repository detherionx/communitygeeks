<section class="closing">
  <div class="wrap">
    <div class="section-marker" style="justify-content:center;">
      {% include "partials/node-mark.njk" %}
      <span class="label">Get in touch</span>
    </div>
    <h2>Talk about your ecosystem.</h2>
    <div class="cta-row">
      <a href="{{ nav.cta.url }}" class="btn-primary">{{ nav.cta.label }}</a>
      <a href="{{ closingCta.secondaryUrl }}" class="btn-secondary">{{ closingCta.secondaryLabel }}</a>
    </div>
  </div>
</section>
