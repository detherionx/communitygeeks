# HANDOFF.md: Communitygeeks Website

**Read this whole file before writing or changing any code.** This is a design-implementation task, not a fresh website-generation prompt. The design is already approved. Your job is to build, refine, and eventually deploy it, not redesign it, unless Carmelito explicitly asks for a redesign.

---

## 0. Production status (read first)

**Since 2026-09-03 the production site at https://communitygeeks.ai is the redesigned "Participation is a living system" site** (Communitygeeks 2.0). Sections 1–33 below describe the previous site and remain as history; sections 34–44 describe how the redesign was built on the review build (`/mockups/field/`); section 45 records the cutover.

- **Deployed from:** `main`, cutover commit `9016a8b` (tag `website-2.0-launch`). Deploys run automatically on push to `main` via `.github/workflows/deploy.yml` (npm ci → `npm run build` → SFTP to Hetzner).
- **Previous production preserved as:** git tag `pre-2026-redesign` = commit `610654a`. No local archive: the old site is fully reproducible from that commit (`git checkout pre-2026-redesign && npm ci && npm run build`).
- **Rollback:** `git revert` the cutover commit (or `git checkout pre-2026-redesign -- src public && commit`) and push; the workflow redeploys. Server-side redirects in `public/.htaccess` would need the same treatment.
- **Layouts:** `layouts/home.njk`, `about.njk`, `contact.njk`, `pt.njk`, all sharing `partials/head.njk` (title, description, canonical, Open Graph / Twitter, favicons, fonts, Organization JSON-LD, extraHead, Umami analytics). There is no `base.njk` any more.
- **Routes:** `/` (chapters `#field #system #approach #questions #cases #thinking #founder-section #talk`), `/about/`, `/contact/` (native form → `/api/contact.php`), `/public-thinking/`, `/public-thinking/<slug>/`, `/de/public-thinking/`, `/de/public-thinking/<slug>/`, `/sitemap.xml`. `/approach/`, `/system/`, `/work/`, `/questions/`, `/mockups/field/*`, `/mockups/home/` redirect (301, `.htaccess`).
- **Visual system:** shell.css tokens: teal `#1F4B4C` (command environment), ink `#1B1912` (structural field), cream `#F6F4EF`, bone `#EDE5D3`, mist `#C0CFCC` (`--constructed`), coral `#E8735A` (`--signal`, the one active colour); Fraunces (display), Archivo (interface/body), IBM Plex Mono (coordinates, indexes). No yellow/ochre on the redesigned pages.
- **Public Thinking identity:** one frontmatter `motif` per piece → `src/lib/constellationMotifs.js` renders it for record | catalogue | masthead | og on dark | paper grounds. Hierarchy: celestial researcher = the practice (animated), constellation motif = one finding (static), journal notation = how findings are recorded. Motifs: `gaming` (canonical controller), `reticulum` (the real constellation; alias `people-beyond-container`). `item.obs` = publication-order number, persistent.
- **Motion grammar (design-system rule):** A ambient/decorative · B narrative/reveal (once, then hold) · C stateful/user-controlled · D task feedback. The trigger must match what the interface appears to be. Reduced motion shows finished states.
- **Deliberate constraints:** static hosting (no server runtime except the PHP contact endpoint); `public/api/contact.config.php` lives only on the server; SFTP deploy does not delete remote files (retired paths are covered by redirects, orphaned assets are harmless); CSP is report-only and the layouts contain one inline `<script>` (the `.js` class) that it would flag if enforced.
- **Where future work starts:** copy comes from the Public Source Pack (Notion); new Public Thinking pieces = one Markdown file in `content/public-thinking/` with `motif`, OG card generated at build; design changes go through prototype → audit → implementation (see the OS Website Changelog for the full history).

## 1. What this project is

Communitygeeks is Carmelito Bauer's ecosystem research and advisory practice. This repo is its public marketing website: a small, static, mostly-text site (five pages) whose main job is to look like a credible boutique research consultancy and to host a growing "Public Thinking" body of research findings, one-pagers, case studies, and field notes.

The site was designed conversationally with a different Claude instance (in claude.ai, with Figma/Notion access), over a long session, then handed to me (a Claude Code instance with no memory of that conversation) to implement. This file is the entire bridge between that session and this one. If something seems unexplained, it's probably genuinely unresolved, check "Section 8: Remaining ambiguities" before guessing.

## 2. Where the design came from

- **Figma file**: `https://www.figma.com/design/uSuq8toZ8FiwWJHz9oHf9V`, was intended to hold 11 pages (one "Design Tokens & Components" page, then a Desktop and Mobile frame for each of the 5 site pages). **As of 2026-08-10, only the "Design Tokens & Components" page is reachable**, Carmelito confirmed this is a sync issue on his end, not a deliberate removal, and asked not to reconstruct the missing 5 pages from this file's prose. Decision (2026-08-10): **this repo's existing HTML/CSS is now the visual source of truth**, not Figma, until Carmelito says Figma access is fixed. The tokens page itself is still reachable and still matches `tokens.json` exactly.
- **Communitygeeks OS → Public Source Pack** (copy source of truth): a Notion page that is the approved boundary between internal research and public-facing language. If you're ever asked to add or change copy, and the answer isn't already in this codebase, that Notion page, not the general Communitygeeks OS, is where to check first. You will not have access to it directly; ask Carmelito to paste the relevant section if you need it.
- **This codebase was built by manually translating the same content into Figma and code in parallel, by the same design session**, not by an automated Figma-to-code export. That means: trust the actual text and values already in this repo as the real approved content. Don't re-derive colors/type/copy from Figma screenshots if you can avoid it, they should already match exactly. If you ever find a discrepancy between what's in this repo and what you can see of the Figma file, that's worth flagging to Carmelito rather than silently picking one.

## 3. Chosen architecture: Eleventy (11ty), static output

**Why Eleventy:** the production constraint is hard, Communitygeeks is hosted on Hetzner legacy static webhosting, so the live site must be plain HTML/CSS/JS with no persistent Node server. Eleventy's whole model is "build static files locally/in CI, deploy the output folder", it needs Node only at build time, never in production. It also stays close to the plain HTML/CSS this site was originally designed in, so the translation from approved mockup to templates was close to 1:1, minimizing the chance of introducing new bugs or drifting from what was approved.

Other options considered and rejected: Astro (more capable, but more opinionated/heavier than this project needs, five mostly-static pages don't need component islands or a JS framework runtime); a plain hand-written HTML site with no build step (would work, but loses the content-collection pattern for Public Thinking, meaning every new piece would require manually duplicating card markup in two places, this is explicitly what Section 6 of the original brief asked to avoid).

**No database, no CMS, no server-side runtime.** Content is Markdown files with frontmatter. This is deliberate, not a shortcut, the brief was explicit about this constraint and there's no concrete requirement here that needs more than that.

## 4. Page inventory (previous site; see section 0 for current routes)

| Page | Route | Template | Notes |
|---|---|---|---|
| Home | `/` | `src/index.njk` | Hero, trust strip, ecosystem diagram, "How we think" question grid, Approach summary, Selected Work, Public Thinking preview (live, pulls 2 newest pieces), Founder, closing CTA |
| About | `/about/` | `src/about.njk` | Founder bio (long version), "How We Think" principles (public-facing, translated from internal Constitution language, see Section 7) |
| Approach | `/approach/` | `src/approach.njk` | Diagnostic question grid, 3 approach categories in detail |
| Contact | `/contact/` | `src/contact.njk` | Direct email + a **non-functional** form mock, see Section 8, item 1 |
| Public Thinking index | `/public-thinking/` | `src/public-thinking/index.njk` | Filterable index: real pieces (from content/) + case-study placeholders (from clients data) + an honest empty-state for Field Notes |
| Public Thinking item | `/public-thinking/{slug}/` | `src/public-thinking/item.njk` | One page per Markdown file in `content/public-thinking/`, via Eleventy pagination |
| Sitemap | `/sitemap.xml` | `src/sitemap.njk` | Auto-generated from `collections.all`, includes new Public Thinking pieces automatically |

Every page shares: a nav (`src/_includes/partials/nav.njk`, active-state highlighting via `page.url`), a footer with the real legal address (`partials/footer.njk`), and, except Contact, which *is* the CTA destination, a closing CTA block driven by front matter (`closingCta.secondaryLabel` / `secondaryUrl`).

## 5. Component inventory

- `partials/node-mark.njk`, the two-circle-plus-bridge-line signature graphic, derived from the actual Communitygeeks icon's glasses shape. Used as a small marker next to section labels throughout. Don't scale it up into a "mascot"; the whole point (per the original design brief) was restraint.
- `partials/ecosystem-diagram.njk`, hand-built SVG diagram (product node connected to Users/Developers/Creators/Partners/Advocates), currently used once, in Home's Problem section. Deliberately not overused, one instance was judged to "solve the problem" of the site otherwise being too text-heavy, per the founder's own call.
- `.pt-card` / `.index-card`, Public Thinking preview cards (Home) and index cards (`/public-thinking/`). Both support a `.placeholder` variant (dashed border, muted styling) for honest "not yet published" states, used for case studies and the field-notes empty state. **Never remove the placeholder styling to make the site look more populated than it is**, that's a deliberate honesty choice already made by the founder, not an oversight.
- `.work-cell`. Selected Work grid cards on Home, data-driven from `src/_data/clients.json`.
- Buttons: `.btn-primary` (filled), `.btn-secondary` (underlined text), also exist as real Figma components (`Button/Primary`, `Button/Secondary`) if you need to cross-check exact padding/sizing.

## 6. Design tokens

`src/_data/tokens.json` has the full color/type/breakpoint values, matching the Figma "Communitygeeks/*" Paint Styles and Text Styles exactly (they were authored identically from the start). The actual CSS custom properties live at the top of `src/assets/css/style.css`, same values, just as CSS variables rather than JSON, because the site doesn't have a build step that would let templates consume tokens.json as CSS (no Sass/PostCSS token pipeline was set up, this was judged unnecessary for a five-page site, but flag it to Carmelito if the site grows enough that hand-syncing two token sources becomes a real maintenance cost).

Fonts: Fraunces (display serif, note it has no "Medium" weight, only Regular/SemiBold; the design uses SemiBold for all display headings), Archivo (body/UI), IBM Plex Mono (dates, tags, confidence labels, data). Loaded from Google Fonts via `<link>` in `layouts/base.njk`, no self-hosting set up. Consider self-hosting for performance/privacy if that becomes a priority; not done here to keep the initial implementation simple.

## 7. Content architecture: Public Thinking

This is the part of the brief most worth understanding properly before touching it.

**Why content/ lives outside src/:** so a non-technical editor (Katharina or Gaza) has exactly one folder to look in when adding a new piece, with zero risk of accidentally touching a template. The tradeoff: Eleventy's collections API only sees files inside the configured input directory (`src`), so `content/public-thinking/*.md` is invisible to `collectionApi.getFilteredByGlob()`, **I initially built this wrong, assuming glob-filtering worked across the whole project; it doesn't, and the build silently produced zero item pages with no error.** The actual fix: `src/_data/publicThinking.js` is a global data file that reads `content/public-thinking/*.md` directly via Node's `fs`, parses frontmatter with `gray-matter`, and renders the Markdown body with `markdown-it`. This is a normal, documented Eleventy pattern for content outside the input directory, not a hack, but it's worth understanding, because if you ever "simplify" this back to a plain `addCollection` + `getFilteredByGlob`, it will silently break again exactly the way it did for me.

**How to add a new piece:** copy `content/public-thinking/TEMPLATE.md.example` to a new file in the same directory (real filename, ending in `.md`, not `.md.example`), fill in the frontmatter, write the body in Markdown. It will automatically appear on `/public-thinking/`, get its own page at `/public-thinking/{slug}/`, appear in the sitemap, and, if it's one of the two newest pieces by date, on Home's preview grid. No template code needs to change.

**The confidence-label safeguard is real and tested**, not just a comment: `src/_data/publicThinking.js` throws a build-breaking error if any content file's `confidence` field isn't exactly `"Observation"`, `"Emerging Pattern"`, or `"Research Finding"`, these are the only public-facing labels approved in the Public Source Pack. Internal OS labels (Low/Medium/High/Validated, or Weak/Emerging/Strong) must never reach this repo. I deliberately broke this during verification (set an invalid value, confirmed the build failed with a clear message, then reverted), see the build log if you want proof this actually works before trusting it.

**CMS decision (2026-08-11): holding off on Decap CMS for now.** It came up as a "still open" item, but adding it means standing up an OAuth backend to authenticate editors against GitHub (Netlify Identity, or a self-hosted proxy), real new infrastructure that cuts against the whole static-site-on-Hetzner rationale in Section 3. With only two placeholder pieces and low expected publishing cadence, the existing copy-template-and-commit workflow is simple enough. Revisit once there are a few real published pieces, or if a non-technical editor needs to publish without going through Git at all.

**The two existing content files are not real yet.** `community-devrel-partnerships-overlap.md` and `moat-or-funnel-diagnostic.md` were introduced as illustrative card copy during the design phase, plausible titles that mirror real research directions in the OS, but never checked against an actual finished writeup. Their bodies currently say exactly that, explicitly, in an HTML comment and in the visible body text. **Do not write real-sounding analysis into these files yourself**, that would be fabricating research findings, which the original brief explicitly prohibited. Wait for Carmelito or Katharina to supply real content, or ask.

## 8. Remaining ambiguities (do not silently resolve these)

1. **Contact form backend: switched to Typeform, embedded (2026-08-11).** Formspree was wired up first (form `xdengeqk`), then replaced same-day once Typeform access became available, Carmelito judged building the form directly in Typeform simpler than hand-maintaining the raw HTML form's fields. `src/contact.njk` now embeds `https://form.typeform.com/to/ScREZMGC` (three fields: name, email, message, same as the old form; no welcome screen, since the embed sits inside the page's own hero copy) via a plain `<iframe>`, not Typeform's embed.js widget, kept intentionally simple, one fewer script for a static site to load. Styled via `.typeform-embed` in style.css (border matching `--line`, fixed height). The Formspree account still exists but is unused; nothing references it anymore.
2. **OG image is placeholder-quality.** `src/assets/images/og-image.png` was generated programmatically (icon + system-font text on the paper background) so that Open Graph tags have *something* real rather than nothing, but it wasn't designed by hand and doesn't use the actual Fraunces/Archivo fonts (a font-rendering pipeline wasn't set up for this). Worth a real design pass before this site is actually promoted on social media.
3. **Favicon set is programmatically generated**, not hand-optimized. Functional, but a designer pass (proper padding, maybe a simplified mark at very small sizes) would likely improve it.
4. **Client logos: sourced 2026-08-11, redesigned same day, see Section 14 ("Logo badge + marquee") for the current system.** First pass used a raw `<img class="work-logo">` per client; Carmelito flagged it as visually broken (wildly differing native sizes/colors clashing) within the hour and asked for a monochrome-by-default, real-color-on-hover treatment instead, applied consistently across Selected Work and a new logo marquee. `work-logo` no longer exists in the codebase, don't reintroduce it. Every logo's provenance (which company, which URL, which naming caveats) is recorded per-entry in `clients.json`'s `logoSourceNote` fields, read those before touching any of this again.
5. **Two Public Thinking content files are placeholder-only**, see Section 7's last paragraph. Don't treat this as "content missing, fill it in"; treat it as "content missing, ask before writing."
6. **Copyright year in the footer (`partials/footer.njk`) is hardcoded to "2026"**, matching what was approved in Figma/HTML at design time. Whether this should become dynamic (always show the current year) is a small product decision nobody's made explicitly, leaving it static for now rather than guessing.
7. **Logo-as-link behavior**: I made the header logo/wordmark a link to `/` (`partials/nav.njk`) since that's near-universal web convention and the original design's brand block was a plain non-interactive `<div>`. This is a small, low-risk assumption, not a silent redesign, but flagging it per the instruction not to invent missing behavior silently.

## 9. Current limitations

- No automated tests (no test framework set up, reasonable for a five-page static marketing site, but worth reconsidering if the Public Thinking section grows to dozens of pieces with more complex logic).
- No image optimization pipeline (Eleventy Image plugin, etc.), `founder.jpg` and other images are served at their source resolution. Worth adding if page-load performance becomes a concern.
- No self-hosted fonts (see Section 6).
- Accessibility: semantic HTML and alt text were applied throughout, and the ecosystem diagram has an `aria-label` describing it, but a full accessibility audit (screen reader pass, color contrast check against WCAG AA, keyboard navigation check) has not been done.
- No analytics of any kind set up.

## 10. Install, run, build

```bash
npm install
npm run dev      # local dev server with live reload, http://localhost:8080
npm run build    # writes production-ready static files to _site/
```

**Build output directory: `_site/`.** Everything in there is what gets deployed, it's plain HTML/CSS/JS/images, ready to be copied directly into the Hetzner static hosting root. Nothing in `_site/` needs a server process to run.

I ran `npm install` and `npm run build` myself before finishing this handoff, fixed two real bugs I found in the process (see Section 7's pagination note, and a separate Eleventy quirk where only the *first* page of a paginated set is added to `collections.all` by default, fixed via `addAllPagesToCollections: true` in `src/public-thinking/item.njk`), and verified every internal link/asset in the built output actually resolves to a real file. The build works as of this handoff. If it doesn't work for you, something changed, check `npm install` completed cleanly first.

## 11. Fixed bug (2026-08-11): missing media-query closing brace

`src/assets/css/style.css` had a missing `}` after the `.pt-grid{grid-template-columns:1fr;}` line inside `@media (max-width:860px){...}`. Everything from the "Shared page-header pattern" comment through the end of the file, all of About, Approach, Contact, and the Public Thinking index page's actual styling (`.index-card`, `.index-grid`, `.filter-bar`, `.filter-pill`, `.card-title`, `.form-mock`, `.contact-grid`, `.principle-list`, `.approach-detail`, etc.), was accidentally nested inside that mobile-only media query, so none of it applied at normal desktop widths. Only discovered after DNS/deploy went live and the real site was checked in a browser; `curl` never caught it because the file transferred byte-identical either way, this was a CSS authoring bug, not a deploy problem. Fixed by closing the media query after the legitimate mobile overrides (line ~140) instead of after all the later content. Verified via computed-style checks in a real browser (not just visual inspection) that `.index-card`, `.contact-grid`, and form inputs pick up their real rules now.

## 12. Fixed bug (2026-08-11): no mobile nav, and two more un-collapsed grids

Two separate mobile-layout problems, found by actually testing at a 375px viewport (not just visual inspection):

1. **The nav bar never had a mobile version at all.** `.navlinks` + `.nav-cta` were plain desktop flex children of `nav` with no collapse behavior, at narrow widths they simply overflowed instead of wrapping, pushing "About" and "Compare notes" off-screen entirely (present but unreachable without horizontal scrolling, this is what looked like "menu links do nothing"). Fixed by adding a real hamburger: `nav.njk` now wraps `.navlinks` + `.nav-cta` in `#nav-menu`, with a `.nav-toggle` button before it; `src/assets/js/nav-toggle.js` toggles a `.open` class + `aria-expanded`; CSS hides `.nav-toggle` and shows `.nav-menu` inline above 860px (the token breakpoint that already existed in `tokens.json` but was never actually wired to anything), and the reverse below it, as an absolutely-positioned dropdown.
2. **`.contact-grid` and `.approach-detail` never collapsed to one column**, no override existed for either, so at mobile widths the two-column grid (`0.85fr 1fr` / `80px 1fr`) squeezed the second column down to ~1px, which for Contact meant the Typeform embed rendered at effectively zero width.

**A cascade-order trap worth remembering**: my first attempt at fixing (2) added the overrides to the *first* `@media (max-width:860px)` block (the one near the top of the file, right after nav/hero). That block sits *before* `.contact-grid`'s and `.approach-detail`'s own base rules further down the file (CONTACT PAGE / APPROACH PAGE sections), and since both rules have equal specificity, the *later* one in source order wins regardless of the media condition being true. The override was being correctly parsed and served, and still doing nothing, which looks exactly like a caching problem until you check rule order specifically. Fix: moved both overrides into the *second*, smaller `@media` block at the very end of the file, which comes after every page-specific section. **Any future mobile override for a selector defined in one of the later page-specific sections must go in that final media block, not the early shared one**, check where the base rule actually sits before adding an override, don't assume "inside any `@media` block" is sufficient.

Verified all of this with real computed-style / `document.documentElement.scrollWidth` checks at a 375px viewport (not just visual inspection) on every page: `/`, `/about/`, `/approach/`, `/contact/`, `/public-thinking/`, and a Public Thinking item page.

## 13. Structured data added (2026-08-11)

Schema.org JSON-LD, per the OS decision in Public Source Pack → "Website Technical Decisions" (decided 8 Aug, never actually coded until now):
- **Organization**, `src/_includes/layouts/base.njk`, sitewide, `@id` `https://communitygeeks.de/#organization`. Description reuses the exact approved sentence from `public/llms.txt`; address reuses the exact legal address already public in the footer. No `sameAs`, no verified social profile URLs exist anywhere in this repo or the Public Source Pack, so none were added; don't add one without a real, approved link.
- **Person** (founder), `src/about.njk`, via the existing (previously unused) `extraHead` front-matter mechanism. `@id` `https://communitygeeks.de/about/#founder`, `worksFor` links back to the Organization `@id`. Bio text is the exact approved About-page version, not paraphrased.
- **Article**, one per Public Thinking piece, computed in `src/_data/publicThinking.js` (as a precomputed `articleJsonLd` string, using `JSON.stringify` for safe escaping, deliberately not hand-templated in Nunjucks, see the bug below) and injected via `item.njk`'s `eleventyComputed.extraHead`. `author` links to the founder `@id` when `piece.author` is "Carmelito Bauer", otherwise a plain `Person` with just a name (no fabricated jobTitle/worksFor for people we don't have public data on).

**Bug found and fixed while wiring this up**: `item.njk`'s `eleventyComputed.title` / `description` (`"{{ piece.title }}"` / `"{{ piece.summary }}"`) were being HTML-escaped at that front-matter templating stage, then escaped *again* when `base.njk` wrote them into `<title>`, `og:title`, `description`, `og:og:description`. Any piece title with an apostrophe (e.g. "...where they don't") rendered as literal `don&amp;#39;t` in the actual page source, broken in the browser tab, social previews, and search snippets, for every Public Thinking piece, since before this session. Fixed by adding `| safe` at the `eleventyComputed` stage so escaping happens exactly once, at the real point of writing HTML in `base.njk`. Same root cause hit `extraHead` while adding the Article schema above, fixed the same way.

Verified all three schema types actually parse as valid JSON in a real browser (not just visually) on Home, About, and both Public Thinking item pages, and confirmed the `@id` links between Article→author→Organization actually resolve to the same object rather than duplicating it.

## 14. Logo badge + marquee (2026-08-11)

Replaced the first-pass client logos (plain `<img>`, wildly inconsistent native sizes/colors, flagged by Carmelito as broken) with a shared `.logo-badge` component used in two places: the "Trusted by industry leaders" marquee (`.trust` section, replacing the old plain-text trust strip entirely) and Selected Work's cards (`.logo-badge-sm`).

**How it works (revised same day, see Section 15 below)**: each badge is a fixed-size box (120×56 in the marquee, 96×40 in Selected Work) containing one layer, `.logo-mono`, the logo rendered via CSS `mask-image`, filled with `var(--ink-soft)`, so it shows as a flat silhouette *regardless of the source image's actual colors*. There is no color-reveal layer anymore (`.logo-color` was removed same day, see Section 15). The logo URL is passed once per instance via an inline `--logo-url` custom property, not duplicated in CSS.

**Why this solves the Space Whale / ZAM problem without touching their artwork**: both have white-only official marks (their sites are dark-background, no light variant exists). `mask-image` only cares about the source image's alpha channel/silhouette, not its actual painted color, so their white shapes work perfectly as a mask filled with our own ink color. Since there's no color-reveal state at all anymore, there's also no risk of their real white pixels ever vanishing against a light background, the silhouette is the only state, permanently.

**Data model**: `clients.json` gained a `logoWall` array (7 entries: DeepL, EU Academy [rendered via the European Commission logo], Tencent, ZAM, Space Whale, Billbee, GB Foods) feeding the marquee. `selectedWork` is unchanged in shape, just renders through `.logo-badge-sm` now instead of `.work-logo` (which no longer exists). ZAM was deliberately added only to `logoWall`, not as a second Selected Work card. Carmelito wanted both marks visible for brand recognition, but the founder's original naming decision (display "Tencent," not "ZAM," in the detailed case-story cards) still stands.

**Marquee mechanics**: `.marquee-track` holds the 7 real badges (each with a `.sr-only` accessible name) followed by an identical `aria-hidden="true"` duplicate set, animated via `transform:translateX` from `0` to `-50%`, since the content is duplicated, `-50%` is exactly where it seamlessly loops. Paused on hover (`animation-play-state:paused`) so a visitor can actually read/hover an individual logo instead of chasing a moving target. Respects `prefers-reduced-motion`.

**A debugging note worth keeping**: while verifying this, `:hover` and forced inline `!important` background changes appeared not to apply at all when checked via this environment's automated browser tool, even though the CSS was correct. Root cause: `.logo-badge` has `transition: background .2s`, and CSS transitions are driven by the same paint/compositor timeline that this specific tool's headless tab wasn't running ("Browser pane is not displayed" blocked every screenshot attempt too). Setting `transition:none !important` on the same element made the change apply instantly, confirming the CSS itself was correct and the failure was purely environmental. If a future session hits the same "my hover/transition style won't apply in the browser tool" symptom, check this before assuming the CSS is wrong.

## 15. Logo badge fixes, round 2: opaque source PNGs, real DeepL logo, dropped color-reveal (2026-08-11)

Same day as Section 14, once the marquee actually went live, three real problems surfaced:

1. **Billbee and GB Foods rendered as solid dark rectangles instead of their logo shape.** Root cause, confirmed by reading actual pixel alpha data (not just PNG format bytes, a file can declare an alpha channel and still have zero real transparency): `gb-foods.png` was **100% opaque** and `billbee.png` was **99.9% opaque**, both source files bundled a solid background tile into the export instead of isolating the mark on a transparent background. `mask-image` needs real transparency to know what shape to cut out; without it, it just paints the whole box. This was a source-asset problem, unrelated to the color-reveal mechanism, it would have broken either way. **Fixed** by removing each file's flat background color programmatically (sampling the dominant background color, zeroing alpha within a tolerance, feathering the edge), this only touches the background, the actual logo artwork/colors are untouched. Also discovered along the way: the *original* `gb-foods.png` download was genuinely corrupt, not a real PNG at all, but a WebP file (`VP8X` chunk signature) mislabeled with a `.png` extension and `Content-Type: image/png` by thegbfoods.com's own CDN. Chrome's `<img>` tag silently content-sniffs and decodes it anyway (which is why it looked fine in casual checks), but a strict PNG parser (Node's `pngjs`) correctly rejected it. Re-downloading didn't help (byte-identical corruption from their server), `sharp` decoded it correctly via format sniffing, same as Chrome, and re-exports a genuinely standards-compliant PNG. If GB Foods' logo ever needs updating again, verify the downloaded bytes are actually PNG (`buf.slice(0,8).toString('hex') === '89504e470d0a1a0a'`) before assuming a `.png` URL served it correctly.
2. **DeepL's logo was "incomplete."** What got sourced originally (Section 4/8) was DeepL's wordmark-only file. DeepL's press page actually has two separate current assets, wordmark and icon, both the same current blue (`#0F2B46`), never combined into one lockup file by DeepL themselves. Carmelito linked a Wikimedia file as a reference, but that's DeepL's *old 2017* logo (different proportions, dated), using it would have meant shipping outdated branding. Fixed by combining DeepL's two *current* official assets into one lockup SVG (`src/assets/images/logos/deepl.svg`, icon scaled and positioned left of the wordmark via nested `<g transform>` groups, original path data untouched). **A real mistake made and fixed while building this**: an XML comment inside the combined SVG contained a literal `--` in the middle of the text ("assets together -- see HANDOFF.md"), which is invalid inside an XML/SVG comment (only allowed immediately before the closing `-->`), this silently broke the `<img>` load entirely. Any future hand-written comment inside an SVG/XML file needs checking for stray `--` sequences.
3. **Dropped the color-reveal/dark-background-flip hover effect entirely**, per Carmelito's direct request, the boldcreators.club reference he pointed to keeps logos monochrome always and only does "some slight effect" on hover, not a color reveal. `.logo-color`, its cross-fade, and the `:hover` background flip to `var(--ink)` are gone from both the CSS and the templates. Hover is now just `border-color` → `var(--ink)` plus a 2px lift (`transform:translateY(-2px)`). This is also strictly safer going forward: there is no longer any state where a white-only mark (Space Whale, ZAM) needs a special dark background to stay visible, the silhouette is the only state that ever renders.

Verified end to end: re-checked actual pixel alpha for all 7 `logoWall` assets after the fixes (all now 56–96% transparent, vs. Billbee/GB Foods at ~0% before), confirmed zero `.logo-color` elements remain in the rendered HTML, and rasterized the combined DeepL SVG through `sharp` to a PNG to visually confirm it actually shows the icon + wordmark together before shipping.

**Round 3, same day: GB Foods rendered tiny relative to the other badges.** `mask-size:contain` scales against the *entire image canvas*, not the visible content, GB Foods' 1840×1476 canvas had enormous transparent padding around the actual ~1102×278 mark (an aspect ratio of 1.25:1 for the canvas vs. 3.96:1 for the real content), so the visible logo rendered far smaller than logos whose source files were already tightly cropped. Fixed with `sharp().trim()` (crops to the actual non-background bounding box, purely mechanical, no artwork altered) on `gb-foods.png` (1840×1476 → 1102×278) and `billbee.png` (280×112 → 205×42, less dramatic but real padding). Left `european-commission.svg`'s modest padding (291×72 → 258×67 trimmed) alone, trimming it would mean rasterizing a vector file for a small gain, not worth the tradeoff. **If any future logo looks disproportionately small or large in a badge, check `sharp('<file>').trim()`'s reported dimensions against the file's actual metadata dimensions before assuming it's a CSS problem**, a mismatch there is almost certainly the real cause, not `mask-size` or the badge CSS itself.

## 16. SEO/GEO audit and first fixes (2026-08-15)

Ran a scoped audit (technical discoverability, semantic/entity legibility, retrieval/citation suitability, off-site) prompted by a LinkedIn post's SEO/GEO claims, but verified those claims against primary sources rather than implementing them as given, several were already going stale (see below). Full findings live in the conversation that produced this; this section covers what actually got implemented.

**Implemented:**
- **`<link rel="canonical">`** added to `base.njk`, pointing at `https://communitygeeks.de{{ page.url }}`. The site was reachable identically on both `communitygeeks.xyz` and `.de` with no explicit canonical signal beyond `og:url`, a real cross-domain duplicate-content gap, now fixed.
- **`topics` frontmatter field** added to the Public Thinking content model (`TEMPLATE.md.example` and both existing placeholder pieces), feeding into `Article.keywords` in the JSON-LD built in Section 13. This is a structured topic/entity signal distinct from `filterType` (which is a *format* label, Research Finding vs. Executive One-Pager, not a topic). Populate it with what a piece is actually about; don't invent a taxonomy that isn't real yet.
- **Heading-convention note added to `TEMPLATE.md.example`**: pieces with more than one distinct sub-claim should give each its own `##` heading. Not enforced by code (a loose convention, not a build-time check like the confidence-vocabulary rule), the item template itself has zero heading scaffolding between the H1 and the closing CTA, so this only helps once someone actually follows it when writing.

**Verified and explicitly rejected from the source post:**
- **llms.txt is not a functioning AI-SEO lever.** Checked actual 2026 crawler-behavior data: major AI crawlers (GPTBot, ClaudeBot, PerplexityBot) essentially never fetch `/llms.txt` (0.1% of AI bot traffic in one measured dataset), and Google's Gary Illyes has publicly compared it to the discredited keywords meta tag. Our `public/llms.txt` stays (harmless, arguably useful for bespoke agents reading it directly), but don't treat it as doing SEO/GEO work in any future planning.
- **"Rank top 10, then get cited" is trending false, not stable.** 2026 data: AI Overview citation overlap with top-10 organic rankings has fallen from ~76% to as low as 17–38% depending on the dataset. Don't treat organic ranking as a reliable prerequisite for AI citation going forward.
- **Did not restructure Public Thinking toward "answer-first, easily-lifted paragraphs" as a goal in itself.** The actual primary research (Princeton's GEO paper) found the strongest citation-boosting tactics were adding statistics, quotations, and cited sources, not paragraph extractability, and content length had ~zero correlation with citation probability separately. Optimizing for "easy to lift" over evidence depth would work against Public Thinking's whole confidence-vocabulary premise for a benefit that isn't what the evidence supports.

**Resolved 2026-08-16:** `communitygeeks.net`, its docroot was already correctly serving the site over plain HTTP (not a code/docroot problem after all), but HTTPS failed because no SSL certificate covered that domain (the server fell back to Hetzner's generic `*.your-server.de` cert, causing an SNI mismatch). Each domain on this account needs its own individually-issued certificate, not a shared multi-domain one, confirmed by comparing against `.xyz`'s working cert (`CN=communitygeeks.xyz`, not a wildcard). Fixed by Carmelito issuing and binding a dedicated certificate for `.net` in konsoleH's SSL Manager (the "SSL-Accounts" binding step, distinct from just having a certificate listed as issued). All three domains now serve identically over HTTPS.

## 17. Public repo security check (2026-08-16)

Carmelito had edited `README.md` to remove the real Hetzner hostname (`www364.your-server.de`) and username (`carmelz`) that had been in it since Section 10, asked me to confirm no other sensitive data existed in this public repo. Checked properly, not just re-reading current files:

- **Confirmed the real password never appears anywhere in git history, in any commit**, `deploy.yml` only ever referenced `${{ secrets.HETZNER_PASSWORD }}`. Checked via `git log --all -p` grepping for the actual hostname/username strings, which confirmed they appeared *only* in `README.md`'s history, nowhere else.
- **The hostname/username fix does not remove them from git history**, editing and committing a new version only changes the current file; the old commit (and everything before it) still shows the real values in GitHub's file history. This wasn't fixed as part of this pass, flagged to Carmelito as needing an explicit decision (history rewrite + force-push vs. accepting the low residual risk, since password never leaked) before acting, given force-pushing is a destructive operation.
- **Found independently (not what was asked about): `clients.json`'s Space Whale `namingNote` names "Digital Mosaik" directly**, the parent entity that's supposed to *never* be named publicly. The website itself correctly never shows this name, but the note explaining that constraint sits in a now-public repo file, which exposes the exact name the constraint exists to protect. Flagged to Carmelito; not yet fixed pending his direction.
- Server IP (`78.46.179.10`, in `HANDOFF.md`) is not a real exposure, identical to what any public DNS lookup on the domains already reveals.
- Repo confirmed public via the GitHub API, zero forks/stars/watchers at check time.

## 18. Marquee overflow on very wide viewports (2026-08-16)

The "Trusted by industry leaders" marquee's seamless-loop technique (duplicate the 7-logo set once, animate `translateX` from `0` to `-50%`) needs each half of the track to be at least as wide as the viewport, or the animation runs out of content mid-cycle and shows a visible gap before looping. With only 7 real clients, one set's width (~1000px) comfortably covers a normal laptop screen but not a large/ultrawide monitor (Carmelito hit this on his own screen; boldcreators.club, the reference for this component, has ~10 logos so didn't show the same problem at the same viewport width).

Fixed with `src/assets/js/marquee.js` (loaded only on the homepage via `extraScripts`): on load, measures the track's actual half-width against `window.innerWidth`, and if insufficient, clones the existing sets (all marked `aria-hidden="true"`) enough times to comfortably exceed the viewport, preserving the "two equal halves" invariant the CSS `-50%` loop depends on, so the loop point stays correct regardless of how many sets end up in each half. Also re-checks on window resize (debounced). Verified at 3440px viewport: half-width grew from 1022px to 5166px (70 total badges), while a normal-width viewport still renders exactly the original 14 with no unnecessary clones. Verified separately that only the original 7 real badges are ever exposed to screen readers, `aria-hidden="true"` on a cloned badge correctly excludes its `.sr-only` name from the accessibility tree regardless of how many raw `.sr-only` elements exist in the DOM, so the clone count growing large is not an accessibility regression.

## 19. Explicit reminder

**Do not redesign the approved site unless Carmelito requests it.** If you notice something that looks like it could be improved visually, that's worth a note to Carmelito, not a unilateral change. Content bugs (wrong copy, broken links, accessibility issues) are fine to fix outright, those aren't design decisions.

## 20. Domain migration to communitygeeks.ai + Umami analytics baseline (2026-08-26)

Canonical domain changed from `communitygeeks.de` to `communitygeeks.ai`, the new primary/production domain. `.de`, `.xyz`, and `.net` remain owned, not decommissioned, they now redirect (see below), with a transition period before that changes further.

**Canonical identity.** Every place the site asserts its own base URL now uses `communitygeeks.ai`: `<link rel="canonical">`, `og:url`/`og:image`, the Organization/Person/ProfilePage/Article JSON-LD `@id`s, `sitemap.xml`, `robots.txt`'s `Sitemap:` line, `llms.txt`, the hreflang alternates on both EN/DE Public Thinking templates, and the founder identity URL in `src/_data/people.js`. Deliberately left unchanged: `carmelito@communitygeeks.de` on `/contact/` (a real, working mailbox, not live URL config, changing it wouldn't move any mail) and this file's own earlier sections documenting the `.de`-as-canonical decision (Section 16) and the `.net` SSL fix (Section 15), historical record, not something to retroactively rewrite.

**Redirect.** `public/.htaccess` (new file), confirmed before writing it that Eleventy's `addPassthroughCopy({"public": "/"})` doesn't filter dotfiles (tested directly: a throwaway `public/.htaccess` did appear in `_site/`) and that `wangyucode/sftp-upload-action`'s upload step has no dotfile exclusion either (checked its actual Python source, `os.walk()`-based, only skips files matching an explicit `exclude` input, which `deploy.yml` doesn't set). One `mod_rewrite` rule enforces a single invariant, scheme `https` **and** host `communitygeeks.ai`; everything else, `.de`, `.xyz`, `.net`, `www.communitygeeks.ai`, plain HTTP on the canonical host itself, gets a single-hop, path- and query-string-preserving 301. Query-string preservation confirmed against Apache's own `mod_rewrite` docs (automatic when the substitution has no literal `?`; `QSA` wasn't needed). Deployed as its own commit *ahead of* the canonical-domain/Umami change specifically so it could be verified live in isolation first, all six domain/scheme combinations tested with real `curl` requests before the second deploy shipped.

**Analytics.** Added a Umami Cloud property (website ID `946e1838-6105-4e34-a6c0-e171c82e7efb`), config isolated to `src/_data/analytics.json` + `src/assets/js/analytics.js` specifically so a future provider swap only touches those two files, not content templates. `data-domains="communitygeeks.ai"` only, deliberately not `.de`/`.xyz`/`.net`/`www`/`localhost`, a direct decision (free tier, one canonical hostname). `data-auto-pageview="false"`; `analytics.js` fires one enriched pageview per load, merging in `slug`/`lang`/`translation_key`/`format`/`confidence` read from `data-artifact-*` attributes present on Public Thinking article pages, and tracks `outbound_click`, attaching source-artifact identity only when the click originates from an article page. The fuller audit this was scoped from, why Umami over PostHog/self-hosted for this specific static-hosting setup, and the UTM convention (`utm_campaign` = the Notion Public Thinking Pipeline's existing `Website Slug` field, so distribution events join to analytics without a new schema anywhere), isn't reproduced here; it lives in that conversation.

**Verified end-to-end in production, 2026-08-26.** All three legacy domains plus `www.communitygeeks.ai` confirmed single-hop 301 to `https://communitygeeks.ai`, path and query string intact, TLS valid independently on each old domain, no loop. Canonical/OG/JSON-LD/sitemap/`robots.txt`/`llms.txt` all confirmed live on `.ai`. The Umami collection endpoint (`https://gateway.umami.is/api/send`) returned `200` for both the homepage and a real Public Thinking article, checked via the browser's own `Performance` API (`responseStatus` on the resource-timing entry) rather than assumed from the request merely being sent, and the exact enriched-pageview payload was captured and matched the article's real frontmatter field-for-field. Carmelito separately confirmed in the Umami dashboard itself that the new property is receiving and displaying this traffic, the one part of this that genuinely couldn't be checked from the deploy pipeline alone.

## 21. Bauhaus-informed visual tightening pass (2026-08-30)

Carmelito produced a standalone pitch-deck document, `src/proposals/tripo/index.html` (Communitygeeks × Tripo EMEA sprint proposal, built earlier the same week, unrelated to this pass), that pushes this repo's existing token palette into a heavier, poster-style register, thick black rules, oversized mono numerals, flat ink/petrol/brass panels. He liked that direction and asked for the *marketing site* (not that document) to be pulled partway toward it, explicitly **not** as a rebrand: no new colors, no new fonts, no IA/copy changes, no new page templates, register kept quiet/editorial. This section is that pass. It followed a Phase 1 audit (presented in chat, not saved to a file) that mapped every proposal-grammar element onto an existing token or component before anything was touched, see that conversation if you need the full reasoning, not reproduced here.

**Two new CSS custom properties**, `src/assets/css/style.css` `:root` (mirrored in `src/_data/tokens.json` under a new `structure` key):
- `--radius:2px`, the button radius that was already hardcoded in two places, now named and reused a third place (see below).
- `--rule-heavy:3px`, a second rule weight alongside the existing 1px `--line` hairline, for top-level structural framing only.

**Radius normalization.** `.btn-primary` and `.nav-cta` now reference `var(--radius)` instead of a hardcoded `2px` (no visual change, just centralized). `.filter-pill` (Public Thinking index) dropped from a 20px pill to `var(--radius)`, the one place a fully-rounded shape existed in the whole system, now a rectangular tab like everything else. Left untouched on purpose: `.pt-figure-link`/`.pt-lightbox-img` (10px/8px/6px, article image framing, not part of the core card system the audit flagged) and `.pt-author-photo` (50%, a circular avatar, a real functional exception).

**Ecosystem diagram sharpened.** `partials/ecosystem-diagram.njk`'s five participant-node rects and the central product rect went from `rx="10"`/`rx="15"` to `rx="2"`, matching the proposal's own diagram, which uses zero rounding. The small circles marking connection points were left alone; they're structural (each one *is* a node), not decorative.

**Removed the decorative hero circles.** `index.njk`'s `.hero-bg-mark` (a near-invisible, 5%-opacity double-circle watermark behind the whole hero, meaning nothing) is gone, along with its now-dead CSS rule. `.hero-art` (the visible two-circle-plus-satellites graphic in the hero's right column) was kept, on inspection it already echoes the same node-mark/ecosystem-diagram grammar (circles as entities, thin lines as relationships, brass dots as connection points), so it's structurally consistent rather than ornamental. Worth a second opinion from André regardless, see the open-questions list below.

**Numbered structures strengthened.** `.q-num` (the "How Communitygeeks Thinks" question grid) and `.approach-idx` (Approach list/detail, both pages) went from ~12-13px quiet mono to 28px/weight 500, the site's existing 01–06/01–03 numbering, just given the visual weight the proposal treats numerals with, using a font weight already loaded (no new font request). Deleted a redundant, fully-duplicate `.approach-detail .approach-idx` override that would otherwise have silently kept the Approach page's numerals small while Home's got the bump (same class, higher-specificity duplicate selector, the two had drifted into an accidental override trap).

**One heavy rule, used once.** `nav{border-bottom:var(--rule-heavy) solid var(--ink);}`, directly under the masthead on every page, mirroring the proposal's own `.bar{border-bottom:3px solid var(--ink)}` under its lockup. Deliberately not applied anywhere else; the token exists for future sparing use, not as a new default border style.

**One flat color panel, used once.** New `.panel-ink` utility (`background:var(--ink);color:var(--paper)`, with scoped overrides so `.node-mark` strokes brass instead of petrol, `.btn-primary` becomes paper-filled, and `.btn-secondary`'s hover shifts to brass instead of petrol, petrol-on-ink has too little contrast to use as a hover accent here). Applied to exactly one place: `partials/closing-cta.njk`'s `<section class="closing panel-ink">`, the site's one full-bleed solid-color block, at its one major register shift. This directly borrows the proposal's `.d-card` flat-fill device without turning the practice into a de-facto new background option, don't reach for `.panel-ink` elsewhere without deciding that's a deliberate second use, not a default.

**Token/CSS drift fixed.** `tokens.json` had documented `displayHero`/`displayH2`/`displayCardTitle` as Fraunces weight 600 since this repo's original design pass; the shipped CSS has always used 500. Per this file's own Section 2 (Figma is not the reliable source of truth; the shipped CSS is), `tokens.json` was corrected to 500 to match what's actually live, the CSS itself wasn't touched, since changing the live weight wasn't part of what was asked for and isn't otherwise broken.

**Inline styles folded into classes**, so token/state changes have one place to happen: `partials/footer.njk` no longer double-wraps its content with a duplicate inline flex layout, the dormant `.footer-address` class (which already existed in CSS but was unused, superseded by an inline-styled `<span>`) now actually does its job, and `footer .wrap{display:flex;...}` in CSS carries the layout instead. `partials/nav.njk`'s active-link/active-CTA state is now a real `.active` class (`.navlinks a.active`, `.nav-cta.active`) instead of inline `style="color:var(--ink)"` / `style="background:var(--ink);color:var(--paper)"` computed per-request in the template. `index.njk`'s hero `h2` strikethrough span now uses a generalized `.old` class (was `.think-intro .old`, scoped to a different section entirely, while the hero used an inline-styled duplicate of the exact same rule, one class now covers both). Removed one fully-redundant inline `style="text-decoration:none"` on `.pt-card` links, the sitewide `a{text-decoration:none}` reset already covered it.

**Verified:** `npm run build` (no test/lint scripts exist in this repo, `build` is the only script). Visually checked Home, Approach, About, and Public Thinking index at desktop and mobile (375px) widths, including the mobile hamburger menu's open state and the nav active-link state on a non-home page. `src/proposals/tripo/index.html` (the proposal document itself, fully isolated with its own inline `<style>`) confirmed unaffected, it was never touched.

**Left deliberately out of scope**, flagged in the audit but not requested: implementing the `390px`/`1440px` breakpoints that exist in `tokens.json` but were never wired into any actual media query (a real gap, but a functional/responsive change, not a token-hygiene one); rounding `.pt-figure-link`/`.pt-lightbox-img`; any change to `.hero-art`.

## 22. Bauhaus pass on the three diagrams (2026-08-30)

Same day as §21, a second pass, this time on the three actual diagrams (homepage ecosystem SVG, and the two Field Note figures), which §21 didn't touch and which still used the previous system's vocabulary: white rounded cards, thin curved teal spokes, large faint dashed halos, symmetric radial layouts. Pure visual translation, no relationships added/removed/renamed, no copy changes beyond what the geometry itself forced (see below), no new colors/fonts. Planned and approved before implementation; full audit/grammar/per-diagram reasoning lived in that plan, not reproduced here.

**Files changed:** `partials/ecosystem-diagram.njk` (homepage), `src/assets/images/public-thinking/fieldnote-ecosystem-map.svg` (Figure 1), `src/assets/images/public-thinking/fieldnote-verb-loop.svg` (Figure 2). No CSS changes needed, all three SVGs are self-contained like before.

**Shared grammar, reusable for future diagrams:** no card chrome for entities, bold Archivo-caps label + small mono descriptor + a small brass circle (the marker convention already used consistently by all three original diagrams, not a new one); straight/angular relationship lines instead of curved beziers; two deliberate line-weight tiers assigned *per diagram* by whichever relationship is the actual point, not hardcoded to center→spoke; a connective-layer device that traces the real shape of the relationship it names instead of a generic decorative halo.

**Homepage diagram:** the product block is now an off-center anchor (was centered), the five participant groups are plain typographic labels arranged asymmetrically around it (was five white cards in a rough pentagon), lines are straight (was curved). Same five groups, same one Creators↔Advocates peer tie, nothing added or removed.

**Figure 1 (ecosystem/network), the real design problem, per Carmelito's own diagnosis:** the old version visually read as COMPANY→six-spokes first even though the caption argues the ties *between* groups are the actual map. Fixed by swapping which edge tier is dominant: the peer-tie hexagon (the network between groups) is now bold solid petrol; the six company-spokes are now the lighter dashed-brass secondary tier (shortened to stubs, see below). The company block stays centered (moving it would misrepresent Andy's model, which does treat it as a real anchor) but is rendered smaller so the hexagon reads as the bigger shape. The community "halo" is no longer a generic dashed circle, it's now a dashed hexagonal frame that traces the peer-hexagon's own shape, with an on-frame "COMMUNITY, CONNECTIVE LAYER" label (the words already existed in the legend below; this surfaces them on the diagram itself). One line of baked-in caption text changed from "the dashed halo below" to "the dashed frame around it", a mechanical correction to keep the caption accurate to the new shape, not a content change; the claim itself (community = connective layer, not a seventh group) is untouched. Legend wording is unchanged; only its two line-style swatches were corrected to match the swapped tiers (they were swapped backwards in an intermediate draft and caught before shipping, worth double-checking swatch↔description pairing if this file is ever touched again).

**A real bug this surfaced, not just a style change:** the old white entity cards weren't just decorative, they were load-bearing. They visually hid the fact that both edge types (direct spokes *and* peer-hexagon edges) terminate exactly at each connection dot, right where the label sits. Removing the cards exposed both: the direct-tie lines ran straight through each group's descriptor text, and the peer-hexagon edges cut through the label text as they approached their endpoint. Fixed by (a) shortening every direct-tie line to stop ~90px short of its dot (reads as a deliberate stub, reinforcing that it's the secondary tier) and (b) pushing every entity label further outward, clear of both the peer-hexagon edge's approach path and the shortened stub. **If any future diagram removes the old white-card treatment, check for this same hidden dependency before assuming it's a pure style swap.**

**Figure 2 (possible lens):** the central "A POSSIBLE LENS" black block is gone, it's now a small unboxed mono annotation off to the side, not a system node the four verbs report to. The four verbs stay uniform in type weight (no size hierarchy, deliberately, so nothing implies Identify matters more than Strengthen) but sit at asymmetric offsets from the ring via short connector stubs, rather than as four identical card slots in a diamond. The four separate arc segments (which, on inspection, were geometrically identical to the faint guide-ring circle behind them, two overlapping redundant devices) are now one continuous solid ring, matching the caption's own "continuous loop" claim more literally than four discrete arcs did.

**Verified:** `npm run build` clean. Checked Home and the `rethinking-where-participation-happens` Field Note at desktop width and at a real 375px mobile viewport (via actual viewport emulation, not just a resized headless window, a plain `--window-size` flag on headless Chrome/Edge does not reliably reproduce the site's real mobile layout and produced a false horizontal-overflow reading during this work; `resize_window`'s mobile preset plus a `scrollWidth`/`innerWidth` check in-page is the reliable way to confirm no overflow). Confirmed the two Field Note figures still behave inside the existing `.pt-figure-link` scroll-frame at mobile (untouched, pre-existing pattern, `min-width:420px` on the image inside a `334px` frame, scrolls internally by design).

## 23. Second Bauhaus pass on the three diagrams, real composition, not just palette (2026-08-30)

Same day as §22. Carmelito's review of that first pass: it read as "remove cards, expose lines, apply cream/petrol/ochre/black", a clean minimalist recolor, not a composition in the Bauhaus-informed editorial language the site and `src/proposals/tripo/index.html` actually use. His test for this round, going forward for any future diagram work too: **strip the color and the word "Bauhaus" from the brief, does the composition itself still read as asymmetric/constructed?** "Uses rectangles/circles and the brand colors" is explicitly not a passing answer. Two design-plan rounds (one revised after review) preceded implementation, not reproduced here in full; the important, reusable part is the discipline that came out of it, below.

**Figure 1 (`fieldnote-ecosystem-map.svg`):** the peer-hexagon-dominant / company-demoted architecture from §22 was kept (it's a genuine information-design improvement, Andy's model made the network between groups visually dominant, not just the six spokes to the company). What changed is the composition around it: a solid petrol plane now runs the full height of the left ~230px of the canvas, carrying the kicker/headline/sub-copy reflowed into a narrow column (the same full-bleed-panel device as `.panel-ink` and Tripo's hero, applied inside a diagram for the first time), text is now part of the plane's construction, not a paragraph floating above an unrelated diagram. The hexagon sits asymmetrically in the remaining paper field, and the company block is pulled off the hexagon's true centroid toward the plane, implying a diagonal axis without drawing an arrow. The "community" frame is one continuous, uniformly-styled hexagonal outline drawn *behind* the plane (so the plane's opaque fill visibly occludes a real stretch of it), **an earlier draft made three of its six edges a heavier solid line and the other three thin dashed, which Carmelito rejected before implementation**: that would have encoded a distinction between relationship types Andy's model doesn't make. The fix was getting the visual integration from compositional occlusion (the plane physically sitting in front) instead of from re-coding what the frame means. General rule that came out of this: a compositional device may crop, occlude, or reshape an existing relationship's *rendering*; it must not give one instance of a repeated relationship a different visual treatment than another instance of the same relationship unless the source material actually distinguishes them.

**Figure 2 (`fieldnote-verb-loop.svg`):** fully rejected and rebuilt, not adjusted, a circle with four labels around a dead center was, in Carmelito's words, "more generic than before" and left `A POSSIBLE LENS` visually stranded. Replaced with four interlocking rectangular fields of deliberately different proportions (Identify a tall column, Understand a wide flat-filled petrol band, Shape and Strengthen filling the remainder) whose own shared edges, not a separate decorative line, form one continuous traceable outline with no start or end marker. `A POSSIBLE LENS / READ IN ANY ORDER, NOT A FIXED SEQUENCE` sits in the genuine interior negative space where three of the four fields meet, rather than floating outside the composition. This is the one diagram in this pass where unequal visual weight across repeated elements (one solid-fill field vs. three bordered ones) was judged *safe*, specifically because the four verbs are actually different kinds of activity (per Andy's own framing) and because nothing about the loop mechanism itself, no arrowheads, identical entry/exit treatment on every field, privileges the filled one as a starting point. Flagged for a visual (not prose) check: whether the solid petrol field ends up reading as "start here" purely from being the most visually dominant panel, independent of that argument, a proportions/rendering question, not something to redesign preemptively.

**Homepage (`partials/ecosystem-diagram.njk`):** rejected once and rebuilt again. The first attempt in this round gave each of the five participant groups a distinct shape *chosen to mean something*, Creators overlapping the product for "direct participation," Advocates as a bottom plinth for "underpins the system," Developers as a full-height column for "structural," etc. Carmelito's objection: this diagram doesn't describe any one studied organization (unlike Figure 1, which transcribes an actual conversation), it's Communitygeeks' generic claim that products generally have these five kinds of groups around them, and asserting a specific configuration among them invents a finding the company hasn't made. The organizing phrase that resolved this, worth reusing elsewhere: **"the structure is definite; the configuration is contingent."** Rebuilt so the product block is the one deliberately privileged, real, asserted object, its own top and left edges extend as the canvas's only structural rules (the grid comes mechanically from the product's own geometry, nothing else invents structure), and the five participant groups are **identical modules** in every dimension that would otherwise carry meaning (size, border weight, fill, label pattern), differing only in *which asymmetric zone the grid happens to place them in*. Distinct-but-equipotent is solved by position, not by ornamental variety per group.

**The cross-diagram discipline this pass established, worth checking before touching any of these three again:** before giving one instance of a repeated element (an edge, a card, a label) different visual treatment than its siblings, ask whether the *source material* actually distinguishes those instances, or whether the distinction would be invented. Figure 2's varied field weights pass this test (the four verbs are genuinely different activities); an earlier Figure 1 draft and an earlier homepage draft both failed it (community's six ties aren't sourced as different in kind; the five homepage groups aren't sourced as differently configured) and were caught before shipping.

**Verified:** `npm run build` clean; both pages checked at desktop width and at a real 375px mobile viewport (same `resize_window` mobile-preset method as §22, a plain headless `--window-size` flag is not reliable for this site's layout). All three diagrams render correctly inside their real page contexts (Archivo/IBM Plex Mono apply via `.ecosystem-diagram`'s CSS classes for the homepage one; the two Field Note SVGs are self-contained with inline `font-family` as before) and the two Field Note figures still behave inside the existing `.pt-figure-link` scroll-frame at mobile, unchanged.

## 24. Figure 1's petrol sidebar reverted; Figure 2 frozen; homepage back to concept stage (2026-08-30)

Same day, third round on these diagrams. Carmelito's read on §23's Figure 1: the petrol sidebar made it worse, not better, it ate too much of the information field, duplicated the article's own title/deck, compressed the hexagon, forced two of six otherwise-identical labels into vertical orientation (an inconsistency with no semantic reason), and covered part of the community frame for a reason that was cosmetic rather than informational. His standing rule for this diagram going forward: **if a visual element can't answer "what does this tell me about the ecosystem?", remove it.**

**Figure 1: sidebar removed, network architecture untouched.** Reverted to the full-width, single-column layout from §22 (top caption band, not a side panel) and re-applied only restrained touches within that architecture: a heavy 3px top/bottom rule (echoing the site's `--rule-heavy`, used exactly once at each edge), a small ochre tick on the company block's left edge (not a fill or a resize), an inline ochre label directly on the community frame, and tightened kicker/legend typography. All six group labels are conventionally horizontal again, no rotated text. The community frame is one continuous, uniformly-styled hexagon around all six groups with nothing occluding any part of it. Company stays small and centered, subordinate to the bold petrol peer-hexagon, exactly as in §22 and §23. Legend/source stayed left-aligned at the bottom (an earlier attempt to shift it into the lower-right corner as an "asymmetric" gesture caused its longest line to run off the 960-wide canvas, reverted; the composition is already asymmetric relative to the centered hexagon without needing to force the legend into a corner too small for its own wording).

**Figure 2: approved as final, frozen.** No changes made or requested beyond a QA re-check (rendered again at desktop and 375px mobile inside the real article page, unchanged from §23, still correct). Do not redesign this one again without a new explicit request; treat further "diagram feels off" feedback on this Field Note as being about Figure 1, not Figure 2, unless stated otherwise.

**Homepage: rejected again, back to concept stage, do not treat §23's version as current.** Carmelito's diagnosis: it had accumulated meaning nobody assigned, a horizontal/vertical axis pair with no defined significance, five modules that read as disconnected rather than as one connected system, an unexplained diagonal peer-tie, and a product block so large it dominated without clarifying anything. His comprehension test for whatever replaces it: **a visitor who hasn't read the surrounding paragraph should get "there's a product, several groups participate around it, and it's one connected system" within about three seconds.** Three thumbnail concepts were proposed in chat (not committed to any file), a black plane with five perforated openings; five overlapping equal translucent fields; a single continuous rule threading through all five plus the product like beads on a string, each answering only: what's seen first, how the product-relation reads, how the group-to-group relation reads, and what makes it Bauhaus-informed without adding invented semantics. **None implemented. The current `partials/ecosystem-diagram.njk` on disk is still §23's rejected version until a concept is chosen and built**, don't assume the file matches the site's actual design intent for this diagram until that happens.

**Verified:** `npm run build` clean; Figure 1 checked at desktop width and at a real 375px mobile viewport inside the actual Field Note page (same `resize_window` method as §22/§23), all six labels legible and horizontal, no text/line collisions, legend fits within the canvas.

## 25. Homepage diagram: Concept B implemented (2026-08-30)

Same day, resolving §24's open homepage question. Of the three thumbnail concepts proposed, Carmelito picked **Concept B (overlapping equal fields)**. A risked implying the five groups are internal constituents of the product (contained inside it); C's single threaded strand invented an unnecessary topology/order between otherwise-equivalent groups. B's semantic mapping was the cleanest: identical modules = no hierarchy, equal intersection with the product = every group relates to it, intersections between fields = groups also relate to each other.

**Implementation, `partials/ecosystem-diagram.njk`:** a full-width black horizontal bar is the product. Five identical rectangles (same size, same fill, same border, same label pattern) straddle the bar vertically by the exact same fixed amount, each spans from 55px above the bar to 55px below it, so every group's overlap with the product is literally, not just approximately, equal. The five are spaced so each overlaps its horizontal neighbor by 30–40px, making group-to-group intersection immediately visible. Carmelito's explicit warning against "stacked browser windows" or a "rectangular Venn diagram" was addressed with small individual rotations (−11° to +9°, varied per module) rather than uniform axis-aligned boxes, enough to read as a constructed composition, not enough to obscure which group is which or vary their weight.

**A real bug found during mobile QA, not just a style issue:** the first working version had descriptor text (e.g. "integrations · tooling" under Developers, "content · education" under Creators) with bounding boxes that *actually overlapped* in the SVG's own coordinate space, invisible at full desktop size where anti-aliasing hid the ~5px intersection, but visibly merging into unreadable text at a real 375px mobile render. Fixed by recomputing every module's horizontal spacing against the longest descriptor string on each side of every gap (not just eyeballing the name labels, which had enough clearance), current gaps are 145/140/150/145px against modules 180px wide. **If any future edit changes these five descriptor strings or their font size, re-check this arithmetic** (half-width of each descriptor's rendered text, summed across each adjacent pair, must stay under the gap between their centers) rather than assuming desktop-only visual review is sufficient.

No explicit peer-tie line (unlike the pre-§23 version's single Creators↔Advocates dashed segment). Concept B's whole premise is that the overlaps themselves carry the group-to-group relationship, generically, for every adjacent pair, which supersedes privileging one specific pair with its own line.

**Verified:** `npm run build` clean. Checked in the real homepage context (not just standalone) at desktop width (1440px) and at a real 375px mobile viewport via `resize_window`'s mobile preset, confirmed no label collisions at either size after the descriptor-spacing fix, and confirmed the diagram still reads as "one product, five overlapping groups" at a glance.

## 26. Homepage diagram: craft refinement pass on Concept B (2026-08-30)

Same day. Carmelito approved §25's Concept B on meaning, semantic structure untouched, not revisited, but flagged execution problems from the actual screenshot: label/descriptor crowding at the top, text running into the tilted panel edges, `YOUR PRODUCT`'s subtitle looking cramped, and rotation angles strong enough that the five fields read as "folded transparent acetate" rather than controlled construction.

**The core fix: the label zone and the panel geometry no longer share vertical space at all.** Previously labels sat at y118–145 while panels started at y100, the panels' rotated edges could cross through where text was rendered. Now panels start at y170 and labels live entirely in y0–170, a zone with zero panel geometry, this is what actually eliminates "geometry competing with text," not just more padding around each label individually.

**Rotation reduced from ±6–11° to ±2–4°.** The wide swings were what read as "randomly folded" rather than "slightly skewed", a controlled Bauhaus tilt should be barely perceptible as a deviation from true, not dramatic.

**Product band grew from 100px to 130px tall**, with the gap between `YOUR PRODUCT` and `PARTICIPATION SYSTEM` increased from 18px to 24px, direct fix for the cramped-subtitle complaint. Panels still straddle the band by an identical 40px above and below (unchanged principle from §25: equal intersection with the product for every group), just recalculated against the new, taller band.

**Everything else is unchanged from §25**: same five groups, same fixed-amount overlap-with-product guarantee, same overlapping-neighbor spacing arithmetic (still validated against each pair's combined descriptor half-widths, per §25's note, the specific numbers shifted since the canvas grew from 820×400 wide but the same check applies), no explicit peer-tie line, no copy/layout changes outside the SVG itself.

**Verified:** `npm run build` clean. Checked in the real homepage context at desktop width (1440px, cropped screenshot showing the homepage section only, per Carmelito's request) and at a real 375px mobile viewport via `resize_window`, labels fully clear of all panel geometry at both sizes, product type has visible breathing room, and the five fields read as a controlled architectural composition rather than a stack of skewed cards.

## 27. Selected Work: composed color pass (2026-08-30)

Same day. Carmelito's observation: after the colored hero, the homepage runs almost entirely cream for several sections. Selected Work (six near-identical white cards) was flagged as a legitimate place to break that, since it's the one section with a real, non-invented reason for color (six discrete pieces of work, not six things needing an invented relationship).

**Implementation is CSS-only**, `style.css`'s `.work-grid`/`.work-cell` rules, plus a few `nth-child` overrides. Neither `index.njk` nor `clients.json` changed; no copy, ordering, or headings touched. This was a deliberate choice, not just convenience: **color/position is assigned by grid position (`nth-child`), never by client identity or sector.** That's what keeps the six-figure count in this section from turning into a de facto classification system (petrol ≠ SaaS, ochre ≠ gaming, etc.), the same principle §24's cross-diagram discipline established for the diagrams applies here too.

**Composition**: three of six cells (positions 1, 3, 5, DeepL, Tencent, Billbee) carry a full flat plane (petrol, brass, ink respectively) with reversed type; this is a diagonal/checkerboard rhythm across the 3×2 grid, deliberately not a row, column, or any grouping a viewer could mistake for a real category. One cell (EU Academy, position 2) carries a quiet `--paper-alt` tint for rhythm. The remaining two (Space Whale, GB Foods) stay plain paper. **Sector was actively checked against the color assignment**: the two "Gaming" entries (Tencent, Space Whale) got different treatments (ochre vs. plain), and the two "SaaS" entries (DeepL, Billbee) also got different treatments (petrol vs. ink), this was verified deliberately, not incidental, specifically so no viewer could infer "color = sector."

**Logo boxes removed sitewide within `.work-grid`** (`.work-grid .logo-badge{background:none;border:none;}`), every mark now sits directly on its cell's own field. The mask-based silhouette technique from §14/§15 (`mask-image` care of transparency, not color) already made this trivial: colored cells just override `.logo-mono`'s fill (`background-color`) to paper on the dark planes (petrol, ink) and to ink on the brass plane for contrast, while the plain-paper cells keep the original `--ink-soft` fill.

**One typographic event, not six**: GB Foods (position 6, plain paper) gets a dramatically larger name (36px vs. 20px), the composition's one deliberate scale outlier, per Carmelito's suggestion. Placed on the *plainest* cell (no color, sparsest copy, "Prior project") rather than a colored one, specifically so this doesn't stack two emphasis techniques onto one client and start reading as "this one matters most."

**The 1px structural grid rule survives automatically**, `.work-grid`'s existing `background:var(--line)` + `gap:1px` technique means the thin rule between cells shows through regardless of what color fills each cell; no new rule/border logic was needed to "preserve" it.

**Verified:** `npm run build` clean. Checked in the real homepage context at desktop width (1440px, cropped to the section) and at a real 375px mobile viewport (single-column stack, same colors/logos, no legibility loss). No other homepage section touched.

## 28. Selected Work: removed the duplicated company-name heading (2026-08-30)

Same day. Carmelito's read on §27 once the color worked: every cell said the company twice in ~50px (logo → serif name heading → description), which read as a generic CMS/portfolio-card stack rather than a modernist composition. Fix: **logo is identity, description is substance, sector tag is metadata**, the written name heading is gone.

**Files touched:** `src/index.njk` (removed the `<div class="work-name">{{ client.name }}</div>` line, one line, no other template change) and `style.css` (removed the now-dead `.work-name` rule and its three color/size overrides, added a consistent logo frame). `clients.json` untouched, the `name` field still feeds the `sr-only` label next to each logo, so screen readers still get the company name even though it's no longer visible text.

**The logo frame, not the logo, is what's standardized.** The six source marks vary enormously in native proportions, DeepL and Billbee are wide wordmark-plus-icon lockups, the European Commission's official mark is tiny, Space Whale is a compact badge, GB Foods is a tall-ish two-part lockup, Tencent is a plain italic wordmark. Rather than force them to a common size (which would have meant stretching some and shrinking others), `.work-grid .logo-badge.logo-badge-sm` is now a fixed 150×52 box with a thin 1px rule and a faint tint, while the logo itself still scales via the existing `mask-size:contain` (unchanged from §14/15's mask-based silhouette system), so every mark keeps its own aspect ratio and just sits centered in an identically-sized frame. Frame border/tint flips to a light plate on the three colored cells (rgba paper at low opacity on petrol/ink, rgba ink at low opacity on brass) so the frame itself stays visible as a bounding rule against every background, not just cream ones.

**Rebalancing after the heading's removal was deliberate, not incidental.** No content was added to fill the gap (explicitly not: enlarging the description, per Carmelito's own instruction), the logo frame's `margin-bottom` went from 14px (old `.logo-badge-sm` default) to 32px, and that's the only spacing change. The resulting extra whitespace between the logo and the description is treated as part of the composition, matching the "emptier cells improve this" read.

**Verified:** `npm run build` clean. Checked in the real homepage context at desktop width, zoomed crops confirm both the European Commission mark and the Space Whale mark (the two smallest/most legibility-sensitive logos) stay crisp and fully recognizable inside the new frame. Checked at a real 375px mobile viewport too (single column, same frame treatment, no collisions), one screenshot at that size briefly showed a stale multi-column render from the *previous* scroll position before the viewport resize fully repainted; a fresh screenshot and a direct `getComputedStyle` check on `.work-grid` confirmed the actual applied layout was already correct (single column, `scrollWidth` matching `innerWidth`), worth remembering if a future mobile check here looks broken on the first screenshot after a `resize_window` call: re-screenshot before concluding there's a real bug.

## 29. `deepl.svg`: interior icon detail was invisible to every consumer, sitewide (2026-08-30)

Same day, found by Carmelito after §28 gave the DeepL logo a bigger, more prominent frame: the hexagon rendered as one solid filled shape, missing the three-dot/arrow detail the real DeepL mark has inside it. **This bug predates this session**, it was there since §15 first combined DeepL's icon and wordmark assets, and would have affected the trust marquee too, just less noticeably at that smaller size; the new logo frame in §28 simply made it visible enough to notice.

**Root cause:** `src/assets/images/logos/deepl.svg`'s icon drew the interior detail (three dot circles + two connecting arrow shapes) as `fill="white"` paths layered on top of the `fill="#0F2B46"` (navy) hexagon, both fully opaque, just different colors. Every consumer of this file goes through `.logo-mono`'s CSS `mask-image` (the sitewide monochrome-silhouette system from §14/15, **this raw file is never displayed with its own colors anywhere on the site**, only ever used as an alpha source), and `mask-image` reads alpha, not color. Since navy and white are both alpha=1, the mask saw one solid opaque hexagon silhouette with no hole, SVG doesn't punch transparency through underlying shapes just because something opaque-but-differently-colored is drawn on top of them.

**Fix:** added a real SVG `<mask>` inside `deepl.svg` itself, white shapes in the mask (the hexagon + its small tail nub) stay filled, black shapes (the three dots + two arrow connectors) become genuine alpha cutouts in the composited raster. Applied that mask to the hexagon paths via `mask="url(#deepl-icon-mask)"`. Because browsers fully rasterize a referenced SVG (including its own internal masks) before CSS reads its alpha channel for the outer `mask-image`, this correctly produces real holes where the interior detail is, which now show through as the cell's own background color, consistent with the same "flat monochrome silhouette" language every other logo already uses here.

**Verified in both places this asset is used**, the Selected Work grid (§27/28's larger frame) and the trust marquee, the hexagon now shows its interior notch/detail in both, at both sizes. `npm run build` clean.

**If `deepl.svg` is ever replaced with a fresh export from DeepL's press page, check first whether the new file already encodes the interior as real transparency** (most modern vector exports do) before assuming this `<mask>` workaround is still needed, it was specifically a symptom of how this particular file was hand-assembled from two separate source assets, not a general requirement for logos in this system. No other logo file was audited for the same issue as part of this fix; if a future logo swap shows a similarly "filled-in" silhouette, this is the first thing to check.

## 30. Social/OG preview image redesigned (2026-08-30)

Same day, per Carmelito's own comment on the "obsolete" LinkedIn preview (a small old wordmark centered on plain cream, visibly disconnected from the site now that §21–29's visual pass had shipped).

**New image**: `src/assets/images/og-image.png` (1200×630, replaces the old placeholder noted as such in §8 item 2, that ambiguity is resolved now). Composition: an asymmetric two-field split, left ~800px is the typographic field (`COMMUNITYGEEKS` kicker with the node-mark + a thin rule, then the two-tone headline "Participation" in ink / "Research & Advisory" in petrol, the exact phrase already used in the homepage hero's black card, kept for consistency, then a mono supporting line, "COMMUNITY · DEVREL · PARTNERSHIPS"); right ~400px is one flat ink plane with the same brass structural tick already established twice elsewhere in this pass (Figure 1's company block, the homepage product bar), plus the node-mark again, small, in `panel-ink` colors (brass solid / paper open) as the one place the mark appears, a secondary signature, not the composition's anchor, per Carmelito's explicit instruction not to reuse the old centered-logo treatment.

**Source kept**: `src/assets/images/og-image-source.svg` is the editable source (viewBox exactly 1200×630, no scaling needed to rasterize); the PNG is a direct headless-browser screenshot of it at 1×. If this image needs updating again, edit the SVG and re-screenshot rather than hand-editing the PNG. It's passed through to `_site/assets/images/` alongside the PNG by the existing blanket `addPassthroughCopy({"src/assets":"assets"})` rule, harmless (an unused-but-public source file, same pattern as the diagram SVGs elsewhere in this repo), not something that needs special-casing out.

**Metadata** (`src/_includes/layouts/base.njk`): added `og:image:width`/`height`/`type`/`alt` (none of these existed before, LinkedIn/Slack unfurlers work without them but render more reliably with them present), and explicit `twitter:title`/`twitter:description`/`twitter:image` (previously relied entirely on Twitter's og: fallback, which works but isn't guaranteed on every platform). `og:title`/`og:description`/page `<title>` were **not** touched, same variables, same values, verified in the built `_site/index.html` output before considering this done. `src/lib/publicThinkingLoader.js`'s `DEFAULT_IMAGE` constant already pointed at `og-image.png` by filename, so every Public Thinking piece's own social card picks up the new design automatically, no separate edit needed there.

**Verified:** rendered the source SVG at full size (1200×630) and at approximately LinkedIn's small link-preview size (~520×273) before finalizing, headline, kicker, and supporting line all stay legible at the small size, which was the actual point of building this as bold flat planes and large type rather than anything finer-grained. `npm run build` clean; confirmed via `file` that the written PNG is genuinely 1200×630 RGB (not, say, an accidentally-scaled or wrong-format output from the screenshot step).

## 31. Figure 1: participant groups converted to node boxes, typography scaled up for real embedded reading (2026-08-30)

Same day. §26's frozen network architecture (six-hexagon peer ties, subordinate company hub, uniform community frame) was sound, but two legibility problems remained once it was actually read at real Field Note width rather than full-screen: the six groups were floating text labels rather than visually distinct nodes, and several secondary text elements (descriptors, the community-layer callout, the company subtitle, the legend, the source line) were sized for a full-screen SVG viewer, not for the ~700px column the figure actually renders at in the article. This was explicitly scoped as a legibility pass, not a conceptual one, no relationships, groups, or semantic status changed.

**Node boxes.** Each of the six group labels is now a square-cornered rectangle: `#EFEBE1` (paper-alt) fill, `#1B1912` 1.5px stroke, group name as 17px bold Archivo, descriptor directly beneath as 12.5px mono, identical stroke weight, fill, and type treatment on all six, so nothing reads as ranked. Box width varies modestly (250px for the two one-line names, Technology Partners, Sponsored Events, vs. 260px for the four two-line names) purely to fit each label without cramping; height varies the same way (100px vs. 128px). No color-coding by group, matching the same discipline §24/§26 already established for this diagram.

**Every line now terminates at a node's boundary, not its old floating vertex-dot.** The peer-tie hexagon segments and the company's direct-tie stubs previously ran to/from bare coordinate points (marked only by a small circle, now removed). With real boxes in the way, each line's endpoint had to be recalculated as the actual intersection of the line with the relevant rectangle's edge, computed geometrically per segment (company-rect and node-rect are both axis-aligned, so this is a straightforward ray/box-edge intersection, not eyeballed) rather than left floating on top of or short of the new boxes. This is what makes the relationships read as genuinely node-to-node once the nodes became visible shapes.

**The community frame grew to keep encompassing the boxes.** The frame hexagon's vertices are offset outward from each node's center along the same radial directions §26 already used, just with the offset magnitude increased (~170px vs. the original ~100px) so the frame clears every box's corner with margin, regardless of whether that box is the smaller one-line size or the larger two-line size. The frame's own style, one continuous uniform dashed stroke, no per-edge distinction, is unchanged.

**Canvas grew and recentered to fit the larger boxes and frame without cramming them**: viewBox went from 960×1220 to 1040×1350, with the whole diagram (frame, boxes, company hub, legend) shifted right and down slightly from its old anchor so the frame's left/right margins stay symmetric. Title/deck/kicker block at the top was left untouched in both position and size, per the "title/deck frozen" constraint, only the diagram body and the legend/source block below it moved or grew.

**Typography increases** (the specific elements the brief called out): descriptors 9.5px → 12.5px; the "COMMUNITY. CONNECTIVE LAYER..." callout 11px → 13px; the COMPANY hub's subtitle 9.5px → 11px (with the hub box itself growing from 150×66 to 170×76 to hold it, and the name line 14px → 16px); legend line text 11px → 13px; source line 10.5px → 12.5px. Legend/source block moved down accordingly (below the taller frame) with proportionally larger line spacing so nothing collides.

**Verified** with the actual QA method the brief specified, not just full-screen inspection: `npm run build` clean; rendered the built article page (`/public-thinking/rethinking-where-participation-happens/`) at real desktop content width (~700px effective column inside a 1440px page) via headless-browser screenshot, every node name, descriptor, the community callout, legend, and source line are legible at that width without zooming into the image. Also checked the real 375px mobile viewport: the figure sits inside its existing `.pt-figure-link` horizontal-scroll frame exactly as before, scrollbar visible, no layout breakage. Figure 2 (`fieldnote-verb-loop.svg`) was not touched and remains frozen per §25/§26.

## 32. Per-piece generated OG/social cards, replacing the sitewide-only image for Public Thinking (2026-09-02)

Triggered by Carmelito noticing article #002's LinkedIn/Telegram preview still showed the generic §30 card. Root cause was two separate things, both fixed here: (1) `base.njk`'s `og:image`/`twitter:image` were hardcoded string literals, never templated at all, so **no page's frontmatter could ever have overridden them, for any piece, ever** (this predates #002, it was latent since §30); and (2) even once templated, a piece's only per-piece image field (`heroImage`) is never rendered anywhere on the article page itself, just fed into the Article JSON-LD `image` field, so it was misnamed for what it actually does.

**Semantic rename, `heroImage` → `ogImage`** (`content/public-thinking/TEMPLATE.md.example`, `src/lib/publicThinkingLoader.js`): confirmed via full-repo grep that nothing renders this field as a visible on-page image, so it's named for its one real job, an optional manual override for the social card. No back-compat shim kept, since nothing else referenced the old name.

**New default: every Public Thinking piece gets its own generated 1200×630 card**, composed from frontmatter alone (`title`, `deck`, `format`, `motif`, `accentColor`), not a photo and not the sitewide logo card. Resolution order in `publicThinkingLoader.js`: explicit `ogImage` override → this piece's generated card at `/assets/images/public-thinking/og/<slug>.png` → (for genuinely non-Public-Thinking pages only, since every PT piece now always resolves to one of the first two) the sitewide `og-image.png` from §30, now wired through `base.njk` for the first time instead of hardcoded.

**Architecture decision, HTML/CSS + Playwright screenshot over SVG+resvg**: explicitly compared against a parallel SVG-template-plus-`resvg`/`sharp` rasterizer. Rejected that path because it would need its own vendored font subset and hand-rolled text-width measurement, a second typography system that can silently drift from the real site's the moment someone changes a font or color token in `style.css`. Chosen instead: the card is a real Eleventy-rendered page (`src/og-cards/card.njk`) that loads the site's actual `style.css` and actual Google Fonts link, so it can never be visually out of sync with the site, at the cost of one Chromium install in CI, checked against this repo's actual deploy pipeline (`.github/workflows/deploy.yml`: plain `ubuntu-latest`, static-file SFTP deploy, no serverless size/cold-start constraint) and judged a non-issue at this content volume (a handful of pieces, growing slowly).

**Pipeline, one `eleventy` build only, no build→generate→rebuild circularity**: `npm run build` is now `eleventy && node scripts/generate-og-images.js`. The single `eleventy` pass renders every real page (each with its final `og:image` URL already pointing at the deterministic per-slug path, computed from frontmatter alone, independent of whether the PNG exists yet) *and* one throwaway fragment page per piece needing a card, at `_site/og-cards/<slug>/index.html` (`src/og-cards/card.njk`, `eleventyExcludeFromCollections: true`, its own minimal `<html>`, no nav/footer, but the real stylesheet/fonts). `scripts/generate-og-images.js` then serves the already-built `_site` over a local HTTP server, screenshots each fragment's `.og-card` element with Playwright at the exact 1200×630 box, and writes the PNG **directly into `_site/assets/images/public-thinking/og/`**, i.e. into the same directory `npm run build` is about to upload, not back into `src/`. That's what avoids needing Eleventy to run twice. The fragment pages are then deleted from `_site` (they exist only to be screenshotted, never meant to be real crawlable URLs). Any failure in this script throws and exits non-zero, no silent fallback to a stale/missing image. No hash-based caching added, two pieces today isn't worth the complexity yet per Carmelito's explicit call; add it if/when generation time actually becomes noticeable.

**Wrapping is real CSS reflow, not character counting**: `.og-headline` and `.og-deck` (new section in `style.css`, "PUBLIC THINKING: OG CARD") are fixed-width blocks that wrap on actual glyph width like any normal text; `.og-deck` uses `-webkit-line-clamp:3` so a long deck (tested against #001's real ~230-character deck) truncates cleanly instead of being force-measured by string length.

**Per-motif opacity, not per-article**: prototyping #001 (`people-beyond-container`, thin-line motif) next to #002 (`gaming`, solid-fill motif) at the same opacity showed the thin-line one reading much fainter. Fixed the same way `.pt-motif--card.pt-motif-gaming` already special-cases opacity for the card context (see §-level precedent already in `style.css`): `.pt-motif--og.pt-motif-gaming{opacity:0.13}` vs `.pt-motif--og.pt-motif-people-beyond-container{opacity:0.26}`, in the shared stylesheet, not a per-piece frontmatter tweak.

**New/changed files**: `src/og-cards/card.njk` (new), `src/_data/ogPieces.js` (new, feeds that template's pagination, filters out pieces with an explicit `ogImage` override), `scripts/generate-og-images.js` (new), `src/lib/publicThinkingLoader.js` (renamed field, added `generatedOgImagePath()`, exposes `ogImage`/`ogImageIsExplicit` per item), `src/public-thinking/item.njk` + `src/de/public-thinking/item.njk` (pass `ogImage`/`ogImageAlt` into `eleventyComputed`), `src/_includes/layouts/base.njk` (og:image/twitter:image/og:image:alt now templated, with the sitewide PNG as literal fallback; fixed a real double-HTML-escaping bug in that fallback string caught during verification, `&amp;` inside a Nunjucks string literal gets re-escaped to `&amp;amp;` when output through `{{ }}`, use a bare `&` in the literal instead), `content/public-thinking/TEMPLATE.md.example` (field renamed and redocumented), `style.css` (new OG-card section), `package.json` (added `playwright` devDependency, `build` script now chains the generator), `.github/workflows/deploy.yml` (added `npx playwright install --with-deps chromium` before the build step).

**Verified**: `npm run build` clean end-to-end locally (Chromium installed via `npx playwright install chromium` first, same as CI now does); all three real pieces (#001, #002, and the German translation `wo-teilhabe-heute-stattfindet`, which has no `motif` set at all) generated correctly, confirmed 1200×630 exactly via `file`; the German piece's title-only card (no motif) confirms that path renders correctly with nothing missing; inspected `_site/public-thinking/{slug}/index.html` for both EN pieces and confirmed `og:image`/`twitter:image`/`og:image:alt` point at the correct per-piece PNG URL; confirmed a genuinely non-PT page (`/about/`) still gets the sitewide `og-image.png` fallback; confirmed `_site/og-cards/` does not exist after a full build (cleaned up); grepped the whole repo for `heroImage`, zero remaining references. Not yet committed, pending Carmelito's review of the generated PNGs.

**Deployed and production-verified (2026-09-02, same day):** committed as `d46be78`, pushed to `main`, CI run `33630487551` succeeded (Playwright install + build + SFTP deploy all green). Live smoke test against the actual deployed site, not just `_site`: `curl`'d `/public-thinking/where-participation-actually-lives/`, `/de/public-thinking/wo-teilhabe-heute-stattfindet/`, and `/about/`, confirmed each `og:image` is the expected absolute URL (per-piece generated PNG for the two PT pages, the sitewide `og-image.png` for `/about/`); fetched all three PNG URLs directly, all HTTP 200, `image/png`, confirmed via `file` as genuinely 1200×630; confirmed `/og-cards/<slug>/` 404s in production (the fragment pages don't leak as real deployed URLs). Any WhatsApp/Telegram/LinkedIn unfurler still showing an old cached preview after this point is a link-preview cache artifact on their end, not a deployment failure, the live metadata and image are confirmed correct at the source.

## 33. `european-commission.svg`: same class of bug as §29, just found later (2026-09-02)

Carmelito flagged the homepage/Selected-Work EU Academy badge as "completely filled" and pointed at the real European Commission logo (flag + 12 gold stars + wave mark + wordmark) as what it should look like.

**Root cause, identical to §29's DeepL bug:** the flag's 12 stars are drawn as fully-opaque `#FFED00` paths layered on top of the fully-opaque `#003399` flag rectangle, both alpha=1. `.logo-mono`'s `mask-image` (§14/15) only reads alpha, so it saw one solid navy rectangle with no distinction where the stars are, same "opaque-but-different-color survives as opaque" failure mode §29 already diagnosed for `deepl.svg`, just never audited on this file (§29 explicitly flagged this as the first thing to check on any future "filled-in silhouette" report, and it was right).

**Fix:** added an internal SVG `<mask id="eu-flag-star-mask">` to `european-commission.svg` (white flag-rectangle shape, black star shapes as real cutouts), applied via `mask="url(#eu-flag-star-mask)"` on the flag path only. The 12 original colored star paths were left untouched and still sit on top, so the file's own colors are unchanged if it's ever rendered directly (e.g. the `<img>` test page used to verify this); the new internal mask only affects what the *outer* CSS `mask-image` sees when it rasterizes the file for `.logo-mono`.

**Scope note, this is a smaller fix than §29's, not a bigger one:** the flag was the only region in this file where two differently-colored opaque shapes actually overlap. The wave-mark and "European Commission" wordmark paths sit beside the flag, not on top of it, so they were never victims of the alpha-blindness bug, they're just fine, thin detail on a 291×72-wide source rendered into a ~150×52 (Selected Work) or ~107×32 (trust marquee) badge. That's expected scale loss for a wide lockup in a compact frame, not something an SVG mask can fix, and wasn't touched.

**Verified:** built a throwaway test page (`public/_test-logo-mask.html`, deleted after use, never committed) with the same `.logo-mono`-style masked div at both the real Selected Work badge size (150×52) and 4× that size for inspection, confirmed the 12 stars now show as real holes (background shows through) rather than one solid blob, at both scales; the wave lines and wordmark are legible at 4×, confirming the fix didn't regress them. `npm run build` clean.

**If another logo ever shows this same "filled-in" symptom, check for overlapping opaque-but-differently-colored shapes first** (per §29's own advice), before assuming the source SVG needs replacing or cropping.

## 34. Concept site ("Participation is a living system") reaches one visual language; Contact gets a native form + own endpoint (2026-09-03)

All of this lives under `/mockups/field/` (noindex, excluded from collections, no analytics). The live site is untouched except for the new, inert `public/api/` folder. Design decisions and their reasoning are in the approval packages Carmelito reviewed (Bauhaus-in-space audit → prototype package → Approach V2 / colour studies → Study D + Contact → backend spike); the short version:

- **Colour language "Study D · tonal orbit / bone product".** Teal `#1F4B4C` field, ivory `#F6F4EF` for participants/paths/lettering, coral `#E8735A` as the single signal, bone `#EDE5D3` for the product rectangle (teal lettering), `#C0CFCC` for constructed surfaces on cream, `#285A5A` for annotation panels. Tokens sit in `shell.css :root` (`--signal`, `--bone`, `--constructed`, `--panel-teal`). **Ochre is retired on the concept pages**: every `var(--ochre)` in field.css / about.css / shell.css now points at `--signal`; ochre remains in `style.css` for the live site and the frozen Public Thinking reading layouts. A rule that fell out of the contrast tests: no colour reaches 3:1 against both teal and cream, so coral is never text on cream and is always paired with ink there; on teal it is used for marks, rules and arcs only; as text it appears only on near-black (≥6:1).
- **Primary control "Concept 3"** (`.kctl` in shell.css): 3px rule + Archivo label + directional mark, colour from `currentColor`, coral only on interaction, teal focus ring on cream. It replaced the circle-and-dot `.ctl*` control everywhere (those rules are deleted). Grammar reason: the circle is reserved for participant groups in the diagrams.
- **Approach V2** (`index.njk` pathway block; `field.js` `drawDesktop`/`drawMobile`; `.v2-*` in field.css): one rising ink vector through three modes of work (dotted lens, three flat constructed fields, continuing loop) on a baseline rule; coral ticks where the path enters a mode; stage names are HTML (`.stage-row-d`, and `.stage-row-m` positioned over the vertical drawing via `--y`); vertical construction at ≤860px. The vector draws once via IntersectionObserver; reduced motion shows the final state.
- **System sequence** runs on `createOrbitField()` in field.js (the hero keeps the old `createField()` engine; the hero is locked by decision). Six states in one orbital vocabulary; stage 5 is a rosette (every path passes through the product), stage 6 the single path with coral exchange arcs. **Labels are HTML** (`.nar-labels` spans positioned by the same 1000×620 coordinates as the SVG and clamped to the safe area); on mobile the label layer is hidden and each stage sets a cropped `viewBox` window. Field notes are tonal panels (`--panel-teal`, ivory text, coral rule).
- **Contact concept** at `/mockups/field/contact/` (`layouts/mockup-contact.njk`, `contact.njk`, `contact.css`, `contact.js`): a native four-stage form (Name → Email → Context → Review and send) inside a "station" drawing; the vector advances per valid stage, the trajectory becomes a solid rule on success. Plain labels; "Send message" is the action, "Request clearance" only an annotation. Without JS every stage is visible and the form posts natively. The shell's Contact links (bar CTA, footer, every "Compare notes") point here; the Public Thinking article closing links still point at the live `/contact/`.
- **Endpoint (decision C-1; Typeform retired for the concept).** `public/api/contact.php` accepts JSON or a plain POST, checks the origin, the honeypot (`website`) and the timing field (`t`), validates (422 with per-field errors), optionally verifies Cloudflare Turnstile, rate-limits per hashed IP (5 per 10 min, temp dir), de-duplicates by the client `id` for 24 h, and sends one email with `Reply-To` set to the visitor, through the Resend HTTP API when `resend_api_key` is configured (Carmelito chose Resend on 2026-09-03; the sending domain must be verified in Resend) or otherwise through authenticated SMTP (implicit TLS on 465 or STARTTLS on 587). **Configuration is `public/api/contact.config.php`, created by hand ON THE SERVER from `contact.config.example.php`; it is gitignored and denied by the folder's `.htaccess`.** Mail routing checked 2026-09-03: `communitygeeks.de` MX → Google, `communitygeeks.ai` MX → Hetzner, so send FROM a Hetzner mailbox on `.ai` TO `carmelito@communitygeeks.de`. The public Turnstile site key goes in `src/_data/contact.json` (empty = Turnstile off). **Not yet tested end to end**: the build machine has no PHP; the front end was verified against mocked 200 / 422 / 429 / 500 / network-failure responses. First deploy test: POST JSON `{"name":"Test","email":"you@example.com","context":"hello","t":0,"id":"t1"}` with an `Origin: https://communitygeeks.ai` header to `https://communitygeeks.ai/api/contact.php`; expect `{"ok":true}` and one email. Without the config file the endpoint answers 503 `not_configured`.
- Also in this pass: About module letters A–D dropped (plain labels), the logo rail becomes a static wrapped row under `prefers-reduced-motion` (P-06), and the mobile nav's active link is cream with a coral left border (coral text on teal would fail AA).

## 35. Refinement pass on the concept site: naming, copy, wide-screen Contact, Public Thinking signal, hero scroll motion (2026-09-03)

- **"Field Note" is editorial only.** The System sequence's note kickers are now `System view 01–06 · …` (six views, not five: the sequence has six states) and the founder kicker reads `Founder · The person behind the work`. Remaining "Field Note" strings on the concept homepage are genuine article references (the two Reading sources that cite a Field Note, the Public Thinking blurb listing formats).
- **Selected Work copy** no longer implies a lifetime total: "Selected engagements across SaaS and AI, the public sector, gaming and FMCG, shown with what was actually done." The six examples are unchanged.
- **Contact on wide screens (≥1600px).** The station box, docking path and form are one composition driven by three custom properties on `.approach-hero` (`--st-w` box width, `--st-vb` viewBox width, `--st-ox` opening-to-edge offset). Above 1600px `contact.js` draws a wider geometry (viewBox `-120 0 1120 620`): a longer approach on the left, the ring and plane whole with a margin to the viewport edge, the opening around 75–78% of the viewport. The form measures about 21rem at 1920, 28rem at 2560 and 33rem at 3440. Below 1600px nothing changed. `matchMedia` redraws the station when the breakpoint is crossed and restores the current step.
- **Public Thinking live-research signal.** A rail SVG (`#pt-signal`, aria-hidden) sits in a 56px column left of the ledger on desktop; `field.js` measures the DOM for one point per row (title centre) and a beacon at "See all Public Thinking", which moved to a `.pt-foot` row beside the "Currently investigating" line as the rail's destination. On entering the viewport the coral trace draws down to the newest finding; hover or focus-within on a row moves it to that row, leaving returns it. The active point pulses every 6s, the beacon every 5s; reduced motion shows the resolved state without animation. On ≤860px the rail is hidden and rows get a coral left edge on hover/focus instead.
- **Hero scroll motion.** `layoutHero(p, mobile, s)` takes normalised scroll progress (0 at top, 1 at 85% of the hero height, smoothstepped) and rotates the constellation about its centroid (14° desktop, 9° mobile) while riding a shallow arc (desktop 40 units across, 50 up; mobile 24/44). Members and links follow; the safe-area sanitizer keeps labels inside; the map layer's scroll parallax (`--py`) rose to 0.12×scrollY with the contours at half rate. Updates run in the existing rAF loop and stop once the hero is more than a viewport away. Reduced motion: no scroll movement. Measured hub travel over the scroll: 41–91px at 1440, 78–146px at 2560, 17–34px on phones; nothing clips the hero container and nothing crosses the headline or controls.
- Verified at 1440×900, 1920×1080, 2560×1440, 3440×1440, 1024×1366, 768×1024, 390×844, 360×800: no horizontal overflow, no script errors, PT hover/focus/leave states correct, reduced-motion states static.

## 36. Concept final pass (2026-09-03): hero orbital choreography + Contact docking assembly

**Hero (`/mockups/field/`, `field.css` + `field.js`).** The sideways drift was replaced by a scroll-linked orbital route. `.hero` is a 170svh stage with a sticky `.hero-pin`; scroll progress over (stage − viewport) drives `layoutHero(p, mobile, e, k)`: the constellation starts upper-right, rotates 42° about its own centroid, scales to `k1`, rises (`--hy`) and then travels 42vw leftwards (`--hx`) into the band between the header bar and the copy block. `heroGeom()` measures that band per viewport (bar height, top of readout/h1, rotated extents of the hubs) so the end state never clips or touches text. Background contours follow at 0.28×, `.topo-fore` contours at ~1.23×. ≤860px: 10° / 6vw, no pinning. Reduced motion: static. Pointer parallax and member drift are kept.

**Contact (`/mockups/field/contact/`, `contact.css` + `contact.js`).** The form is now `--dock-w: clamp(15rem, 24vw, 28rem)`. Desktop station geometry (`drawStation`, viewBox 760×620, or `-120 0 1000/1080 620` from `--st-vb` at ≥1600 / ≥2400px): partial ring cx515 cy330 r215 with two heavy segments, support plate 330/400 250×200 under the form's lower-left, vertical registration rail x372 with four indexed marks (coral for completed/current stage), horizontal approach line at y330 ending on the form's left edge with the coral arrival signal. `--st-ox` (60 / 180 / 260) keeps the form inward at wide widths. Mobile header composition unchanged.

**QA (`scratchpad/verify-final.js`, eight viewports):** no JS errors, no horizontal overflow, no text overlap at any of six scroll frames, min clearance ≥52px, travel 42% of viewport at all desktop widths, rotation 41°, reduced-motion static; Contact ring right margin 35px@1440, 25px@1024, ≥224px at ≥1920; docking point on the form edge everywhere.

## 37. Public Thinking field journal (2026-09-03)

The upper-right paragraph is gone; the contextual copy sits under the heading as HTML (`.pt-sub`: "Research findings, executive one-pagers, case studies and field notes—tagged by format and by how much evidence currently supports them."). In its place `.pt-head-side` holds `#journal-svg`, an aria-hidden instrument built and driven by `field.js` (block "PUBLIC THINKING: FIELD JOURNAL + RAIL").

**Geometry (`build()`):** variants desktop / tablet (≤1100, no measurement scale) / mobile (≤860, viewBox 360×212, cropped page, shorter path). Bone page (`--bone`) on a `--panel-teal` console panel offset lower-right, approach rail from the left edge, teal margin rule and 24-unit ruled grid, ivory registration corners and scale, a dock bracket with a coral status square. Ink marks in writing order: `index`, `note`, `obs` (circle + core), `conn`, `pattern` (triangle + core), `arrow`; each is a path revealed by stroke-dashoffset. The pen is an engineered group (coral tip, ivory body, teal band) positioned at `getPointAtLength` with tangent-based rotation; between marks it lifts along a move segment; toward the dock it straightens into its resting pose.

**Choreography (`measure()` → document-space anchors, `journal.update(sy, vh)` per frame, no layout reads):** registration `r` runs from head top at 0.92vh to 0.6vh (panel → page → grid/registration → pen approach). Writing timeline `T` maps scroll piecewise: [r1, s1] → through the circled observation (s1 = first row at 0.74vh), [s1, s2] → connection + pattern (s2 = second row), [s2, s3] → arrow + dock (s3 = See all fully visible), minimum gaps 0.1vh, compressed if the page ends early. Rows get `.rec` (opacity .78 → 1) as recorded; `.journal.done` starts the status pulse and `.pt.in` the rail beacon. Hover/focus on a row flashes its marks (`.hot`) and moves the rail trace; no replay. Reduced motion: completed state, pen resting, no pulses. Work stops outside [r0 − vh, s3 + 1.5vh].

**QA:** `scratchpad/verify-journal.js` (8 viewports × 6 scroll states, reduced motion, hover): no JS errors, no overflow, pen inside its box at every state, first row records with the circle, second with the pattern, completion at/after See all, mobile/tablet variants correct.

**Update (2026-09-03, later):** the field-journal treatment in §37 was rejected on review ("amateurish, does not fit the theme") and reverted; the section is back to the §35 state (paragraph upper-right, live-research rail). New direction under prototyping: a celestial constellation field (Algalon-like mood translated into the Communitygeeks grammar), three static compositions A/B/C to be approved before any animation is implemented. Prototype script: `scratchpad/proto-constellation.js`.

## 38. Public Thinking: the celestial researcher, static review build (2026-09-03)

Direction approved after the constellation studies (§37 update): a presence built only from constellations holds an open ivory field journal in one hand and writes into it with the other, Algalon as the mood reference (humanoid anatomy first, celestial material second), no body, face or costume; both forearms leave the frame. This build is STATIC (no animation yet) so anatomy can be reviewed on the site.

**Files.** `src/assets/js/researcher.js` (builder; `window.CGResearcher.scene(svg, {mode, view})`, auto-boots on `svg[data-researcher]`, rebuilds on crossing the 860px breakpoint), the `.pt-cel` / `.cel` block in `field.css`, the Public Thinking section in `index.njk` (background star svg + `.pt-field` scene svg left 59% + `.pt-edit` column right 39%; rail markup kept but hidden), review page `src/mockups/field/researcher.njk` → `/mockups/field/researcher/` (close-ups A holding hand, B writing hand, each beside a construction view with solid volumes and no stars).

**Anatomy (scene viewBox 760×1000, journal at translate(380,500) rotate(-7), pages 188×260).** Holding hand, first person: thumb on the front of the left page from the lower-left corner (`HOLD.thumb`), fingers behind the paper (hidden), palm heel below the bottom edge and little-finger side past the left edge, wrist at (236,770)/(162,760), forearm out bottom-left. Writing hand in the pen frame (tip T=(442,512), axis u=(0.62,0.78) toward the elbow, side s): tripod grip (thumb along the lower-left of the barrel, index arcing over, middle under), ring/pinky curled, wrist P(158,24)/P(158,92), forearm out lower-right; pen 150 long. Layers per hand group: `.sil` translucent volume (opacity .11; solid in construction mode), `.struct` constellation lines, dust, `.nodes` (wrist star with rays/rings). Anything in front of the paper is cloned with `.onpage` (teal) and clipped to the pages; the original is masked off the pages.

**Views.** `full` (desktop), `mobile` (`150 330 500 520`, field 104vw tall), `a` (`90 440 320 400`), `b` (`400 470 320 260`). QA: `scratchpad/verify-researcher.js` (section 1440/1920/390, review page; no JS errors, no overflow).

**Next.** Anatomy sign-off → scroll choreography (0–15 hand resolves, 15–30 journal settles + writing hand arrives, 30–70 writing, 70–85 sketch + coral note, 85–100 pen lifts; reduced motion = finished scene).

**Anatomy pass 2 (2026-09-03, deployed).** Holding arm rebuilt as one smooth Catmull-Rom silhouette (`HOLD.outline`: forearm taper → narrow wrist → hypothenar bulge past the page's left edge and thenar mass on the thumb side), thumb shortened and bowed with a pad ellipse and the metacarpal inside the thenar mass; contact evidence added in the journal group (`.contact` shadow of the book edge on the palm, `.contact-pad` pressure mark under the thumb). Writing hand: knuckle arc flattened slightly, ring/little finger curled back toward the palm. Construction mode is now deliberately plain (solid grey volumes, joints only, no constellation lines). Review page gained C (full desktop scene) and D (390px crop). Celestial/unfilled is the approved final treatment; construction stays a QA tool.

## 39. Celestial researcher: scroll choreography (2026-09-03, deployed)

Anatomy signed off; the Public Thinking scene is now scroll-driven (`researcher.js`, `{"part":"scene","animate":true}` on `#researcher-scene`). Everything is a pure function of progress p, so scrolling back unwinds; no loop, no idle motion.

**Progress.** Anchored to the journal, not the section: p = 0 when the journal's top edge enters at 98% of the viewport height, p = 1 when it has risen to 10% (`jf` = journal top as a fraction of the field: 0.346 desktop, 0.035 mobile crop). Layout is measured on load/resize/fonts-ready only; scroll handler is passive and rAF-throttled; per frame only `scrollY` is read.

**Phases.** 0–0.15 holding arm resolves (opacity + 40/60px translate from lower left). 0.12–0.30 journal settles (opacity, 18px drop; the clip/mask page rects move with it) and the thumb fades in with it; 0.15–0.30 the writing hand and pen arrive along the forearm direction (90/110px). 0.30–0.85 writing programme `segs` on the right page in order: "OBS. 002 · 2 SEP" (text, clip-rect reveal), "PARTICIPATION", underline, node A, line A–B, node B, curve A–C, node C, "≠ CONTAINER", coral circle around C, then a move to the rest point; short lifted moves between items; strokes use `0.5u + 0.5·smoothstep(u)` for an authored rhythm. The pen tip follows the revealed point (`getPointAtLength` / text width × u) with a ±5° tangent tilt; the whole writing hand translates with the tip. 0.85–1 the pen lifts 3/−7px and rests. Reduced motion: `apply(1)` once, no controller.

**Structure note.** `dual()` now wraps each in-front group: the wrapper carries mask/clip in scene space, the inner group (and its on-page clone) moves. Text reveal uses per-text clipPaths (`<id>-t0..2`); lengths measured via `getComputedTextLength` after fonts load, with a fallback estimate.

**QA.** `scratchpad/verify-anim.js`: 1440/1920/2560/390, nine progress points each: opacities and reveals monotonic, pen inside the field at every point, no JS errors, no overflow; reduced motion complete. Review page text updated (frames static, section animated).

**Follow-up (2026-09-03, deployed).** Public Thinking: six faint constellation clusters in the background star field on two layers (`.cons-a`, `.cons-b`) that drift with the section's scroll progress (−46/−92 viewBox units vertically, slight horizontal counter-drift), driven from the same controller; static under reduced motion. Hero on mobile: the constellation now turns 40° (was 10°) over the first half of the hero's height, contracts to k 0.8, travels 4vw, and breathes with a slow ±2.5° ambient oscillation when the page is still; desktop unchanged.

**Mobile hero pinned (2026-09-03, deployed).** On viewports ≤860px wide and ≥740px tall the hero is pinned like the desktop one (`.hero` 160svh, `.hero-pin` sticky 100svh, copy bottom-aligned, constellation box 34svh under the bar); `heroGeom()` now measures the mobile case too (base HERO_MOBILE, k1 ≤ 0.92, no horizontal travel) so the 40° turn and contraction stay clear of the readout (≥17px at 360×800). Shorter phones keep the unpinned flow with the turn over the first half of the hero. QA: `scratchpad/verify-mobile-pin.js`.

## 40. UX/logic audit fixes (2026-09-03, review build)

Audit (report in the session's `scratchpad/pkg-audit.html`, decisions by the founder): F1, F2, F3, F5, F4a, F7, F6 implemented; P3 chapter index HELD; F8 observe only.

- **F1 System stage, height-aware** (`field.css .nar-box`): `width:min(88vw,1200px,calc((100svh − bar − 44px) × 1000/620))`, centred in the space under the bar, at 58% from the left below 1700px (52% above); notes `min(400px,30vw)`. 1366×768: 1200×744 (97% vh, 33px under the bar) → 1071×664 with 28/16px clearance; 1920 unchanged.
- **F2 pinned distance**: `.nar-step` 64vh → 52vh, tail 40vh → 30vh. 1366: pinned 2765 → 2227px; page 12.1 → 11.4 vh.
- **F3 mobile System = stateful instrument**: `#nar-tabs` tablist (01–06 buttons, mono numerals with a coral square marker on a baseline rule) + Next control (`.nar-tab-next`, Concept-3 mark) + live label "View 0N of 06 · name"; figure in normal flow (44svh, min 230px, `overflow:hidden`); one note shown (`.js .note:not(.on){display:none}` + `hidden`); arrow/Home/End keys; scroll never changes state on phones (`update()` only drives stages when `!narrow()`); `setNarStage(0)` at init; no-JS: tabs hidden, notes stack. 390: section 1975 → 658px; page 10604 → 9521px.
- **F5 200% reflow**: mobile `.nar-box{height:calc(100% − 64px);width:auto;max-width:calc(100% − 24px)}` inside a clipped stage; 683×384 box 268×166 inside the stage.
- **F4a anchors**: `#approach,#questions,#cases,#thinking,#founder-section,#talk{scroll-margin-top:var(--bar-h)}` (not #system, whose pinned stage is built to sit under the bar). Bar links land at 60px.
- **F7 tablet hero**: constellation box 44svh at 700–860px width (768×1024: 451px, 90px above the readout).
- **F6 logo band settles**: marquee, mask and duplicate copies removed; the seven logos wrap centred and slide into place once (`.trust.in` via IntersectionObserver, staggered 60ms); reduced motion: present immediately.
- **QA**: `scratchpad/verify-impl.js` (laptop geometry, mobile tap/Next/keyboard/scroll, 200%, no-JS, reduced motion, anchors, tablet hero, logo band) and `verify-anim.js` regression: all pass, no JS errors, no overflow.

**System prose + phone counter (2026-09-03, review build).** Designer feedback (André Batista) confirmed the interactive mechanisms; his one correction applied: `.note p:not(.note-lead)` now 15.5px/1.6 at full opacity on desktop (site paragraph size; 16px on ≤860 via the existing rule), the lead, kicker, tags and verb labels unchanged. `.nar-hud` hidden on ≤860 (the scale and "View 0N of 06" label carry the state; the corner counter sat under the header when anchor-landed). Verified at 390, 430, 768, 683×384 and 1366: prose equals the standard paragraph size, notes sit below the controls, tap/Next/keyboard work, reading scroll keeps the state, reduced motion and no-JS fallbacks intact (`scratchpad/verify-prose.js`).

## 41. Public Thinking V2: constellation identities (2026-09-03, review build)

Approved system (board + refinement): the celestial researcher = the practice (animated, homepage only); a **constellation motif** = one captured finding (static, same geometry on every surface); journal notation = how findings are recorded. Decisions: D1 identity on four surfaces; D2 motif heads the article notation column (never behind the title); D3 homepage motif weight ~90% line opacity; D4 `accentColor` retired from Public Thinking presentation, field kept; D5 OBS numbers by publication order, persistent.

**Renderer.** `src/lib/constellationMotifs.js` (UMD; `motifSvg(kind, ctx, ground)` → SVG string; motifs `gaming` = ghost-volume controller with two shoulder stars + primary face button as majors, precise d-pad/button geometry, coral ring on the top button; `people-beyond-container` = broken boundary, three bodies inside, four outside, coral ring on the outside body). Exposed as the Nunjucks filter `constellation` (.eleventy.js) and the macro `ptConstellation(motif, ctx, ground)` in `partials/pt-motifs.njk`. Contexts: record (dark), catalogue/masthead/og (paper). Styling: `.cm*` block appended to style.css and field.css. The old `ptMotif()` watermark stays for the live (non-concept) pages: `/`, `/public-thinking/`, live articles unchanged.

**Loader.** `item.obs` = zero-padded publication-order number per translationKey (earliest date first): rethinking… = 001 (EN+DE), where-participation… = 002 (EN+DE).

**Surfaces (concept shell).** Homepage `index.njk`: `.rec-motif` per ledger row (record/dark), sticky hover/focus rail and coral-title variant removed; response = constellation gains presence + CTA underline 2px, gated by `@media (hover:hover)` and `:has(a:focus-visible)` (field.css). Archive `pt-card-base.njk`: catalogue glyph top-right + `.cat-no` "OBS. 00N" (pt-shell.css; accentColor neutralised there). Articles `pt-item.njk` / `de-pt-item.njk`: `.obs-label` ("OBS. 002 · 2 Sep 2026 · transferred from the field" / "aus dem Feld übertragen") above the title; `.pt-motif--spec` heads the side column (138% width, cropped by the page); side column transparent with ruled notation, coral rule + "Open question"/"Offene Frage" label via `html[lang]`. OG `card.njk`: og/paper motif at full presence, headline/deck columns narrowed (style.css). OG PNGs regenerate in the existing `npm run build`.

**QA** (`scratchpad/verify-ptv2.js`): motifs present on both records, hover/focus response measured, tap leaves rest state; archive EN/DE glyphs + numbers (numbers hidden on phones); mastheads EN/DE; OG fragments; live pages carry no `.cm` markup.

## 42. About: operator dossier → research navigation chart; homepage: engagement manifest (2026-09-03, review build)

**About (`about.njk`, `about.css`, `about.js`).** One coordinate system: `--course-x` (column 9's left edge, the portrait's left edge; column 8 on tablets; 22px on phones) is a registration axis that runs from the operator module through the dossier (`.axis-founder`, ticks, one coral point at the contact links) into the dark field. Hero = operator identification field: the practice's field chart (four labelled coordinates Participation/Research/Advisory/Ecosystems + Community, Developer relations, Partnerships as supporting points; one plotted line; HTML labels placed by `about.js`), the portrait plate with corner marks, one coral registration point (lower-left, the course's origin) and the mono index `OP · 01 · REG CGK-A`. Dossier = asymmetric record: name/role cols 1–5, bio + links cols 9–12 on the axis. Chart = `#think-chart` with an SVG course (viewBox 1000×1000, non-scaling strokes): the route follows the axis down past the heading, then plots 01 (lower-left) → 02 (centre) → 03 (upper-right) and continues to the handoff, where `.course-tail` receives it; nodes, rings, ticks and mid-segment points are computed; the coral signal travels the route and rests on 03; principles are absolutely placed (`--x/--y/--w`) and activate as the signal reaches their node. Progress: chart top from 85% vh to 20% vh (or its bottom in view). Tablet: same with a steeper course; phones: `.course` hidden, a vertical rail with `::before` nodes and a `.think-signal` moved by scroll, prose in flow. Reduced motion / no-JS: full chart, all principles visible. Footer stays beige. Copy unchanged.

**Engagement manifest (`index.njk` cases, `field.css`, `field.js`).** Six logo coordinates on one rail (`role=tablist`, buttons with accessible names), a coral cursor (`--i`) with a registration square, the active logo on a bone plane, one shared record (`.mani-rec.on`: `01 / 06`, description, sector) with a fixed height from the tallest record; hover previews on pointer devices, click/keyboard selects, arrows/Home/End move; entrance once via IntersectionObserver; no autoplay. Logos normalised per mark (`--lh`: DeepL 22, EC 30, Tencent 19, Space Whale 40, Billbee 22, GB Foods 24; smaller on phones), same assets, no stretching; Space Whale's official white SVG is used through the mask. No-JS: all six records stack. Section height 1440: 589 → 375px (−36%); 390: 843 → 561px.

**QA** (`scratchpad/verify-about-manifest.js`, 10 viewports incl. 683×384 = 200%): no JS errors, no overflow, portrait never clipped, field chart never over copy, no principle/heading overlaps, course drawn and principles active at completion, beige footer; manifest click/keyboard/hover states, record height stable, reduced motion and no-JS fallbacks. Homepage researcher regression unchanged.

## 43. Pre-launch corrections: canonical controller, Reticulum, Communitygeeks 2.0 social preview (2026-09-03, review build)

**Controller (`src/lib/constellationMotifs.js`, motif `gaming`).** One canonical drawing for every surface: a closed exterior contour (~240×140 units in a 300×220 viewBox, 1.71:1, inset ≥28 units) with compact shoulders, subtle grips and a shallow lower indentation, plus d-pad cross and four-button diamond; the coral ring is the observation. `.cm-contour` strokes it, `.cm-body` fills it faintly. Every SVG carries `preserveAspectRatio="xMidYMid meet"`; all strokes use `vector-effect:non-scaling-stroke`. Containers no longer crop or offset: `.rec-motif` (homepage record) sits inside the row, right-aligned and vertically centred at `min(34%,210px)` (phones/tablets: its own grid cell beside date and kicker, `min(100%,150px)`); `.pt-motif--spec` (masthead) is 100% of its column with no negative margins; `.pt-motif--og` sits 56/52px inside the card at 42%. QA (`scratchpad/verify-final.js`): drawing inside its SVG and SVG inside its clipping ancestor at 1440/1024/390/683×384 on homepage, archive, EN/DE mastheads; no overflow.

**Reticulum (motif `reticulum`).** The second finding's motif is the real constellation Reticulum (Ret, "the Reticle"): alpha–beta–delta–epsilon plotted from J2000 positions as the IAU stick figure (closed quadrilateral), gamma/iota/theta/kappa as unconnected field points, the zeta pair carrying the coral observation outside the figure, a faint reticle cross at the centroid, label `RET · RETICULUM / THE RETICLE` (masthead and OG only). Frontmatter of both language variants now says `motif: "reticulum"`; the old slug stays as an alias, and the legacy `ptMotif()` watermark on live pages is untouched. Accessibility: the masthead specimen is `role="img"` with an EN/DE description (filter/macro take a `lang` argument); record, archive and OG copies stay `aria-hidden`. The choice and its references are documented in the renderer's header comment.

**Social preview.** `src/og-cards/site.njk` → `scripts/generate-og-images.js` screenshots it at 1200×627 into `assets/images/communitygeeks-2-og-2026.png` (new filename so LinkedIn/Buffer caches keyed to `og-image.png` cannot serve the old artwork). Deep teal, bone Fraunces, coral square, Reticulum right. `layouts/mockup-field.njk` now emits the full og:/twitter: set with absolute URLs (`og:url` from `page.url`, 1200×627, alt text, `summary_large_image`); the concept homepage sets `ogTitle`/`ogDescription`. **Production `layouts/base.njk` was deliberately left untouched** (still `og-image.png`, 630): at launch, point its `og:image`/`twitter:image` defaults to `communitygeeks-2-og-2026.png`, set `og:image:height` to 627 for the default, update `og:image:alt`, add `twitter:image:alt` (prepared diff: scratchpad `base-njk-launch.patch`; the mockup layout already shows the intended values). Post-launch checklist: open https://communitygeeks.ai/, view source and confirm the meta set; LinkedIn Post Inspector (linkedin.com/post-inspector) → inspect the URL to refresh the cache; in Buffer remove and re-add the link (or refresh the preview) and confirm the new card.

## 44. About: one research navigation instrument (2026-09-03, review build)

Founder feedback on §42: the scattered constellation graphics read as decoration. Rebuilt `about.njk` / `about.css` / `about.js` from one governing idea: *the research practice is the central instrument through which three principles are plotted.* Copy, URLs, links, heading order and the cream footer are unchanged.

**Removed:** the hero field chart (labelled coordinates + plotted line), the dossier axis with its coral point, the diagonal course through "How we think", the mobile vertical rail with travelling signal, the handoff course tail. The page now has exactly one constellation-style element.

**Hero:** calm identification screen. Headline + definition cols 1–7, rectangular portrait plate cols 9–12 (max 460px, so 1920 does not inflate it), one registration axis at the plate's left edge (col 9; col 8 on tablets; hidden ≤700), corner marks + `OP · 01 · REG CGK-A` + one coral registration point kept. Deck height 811 / 741 / 697px at 1920 / 1440 / 1366.

**Instrument (`#instrument`, aria-hidden):** hub square with two calibration rings and four ticks, one dashed-free elliptical orbit, axes, three nodes plotted *on the orbit* (01 lower-left, 02 apex, 03 lower-right), spokes hub → node, mono labels `01 · ECOSYSTEM` / `02 · RESEARCH` / `03 · CHANGE`, `RESEARCH PRACTICE` under the hub, a notation line `HUB 500 · 150 / ORBIT 400 × 120 / 03 NODES`, one coral signal at the hub. Wide geometry 1000×300 is server-rendered; node x = 1/6, 1/2, 5/6 so each node sits exactly above its principle column (measured: node and column centres identical at 1920/1440/1366). `about.js` swaps to a 1000×760 geometry ≤860px. Colours: ivory primary, mist (`--constructed`) secondary, coral signal; no yellow.

**Grid:** principles in three columns under the instrument with fine vertical rules and a top rule (no cards); stacked with horizontal rules ≤860. Deck/dossier keep two columns down to 700px.

**Calibration routine (CSS transitions, `.ready` added once by IntersectionObserver at 30%):** 0–0.7s orbit, axes, calibration, notation resolve → 0.35s hub + signal → 0.55/0.75/0.95s spokes draw hub→node (`pathLength=1` dash) → 1.1/1.3/1.5s nodes, rings, labels 01/02/03 → 1.15/1.35/1.55s principle columns. Holds afterwards (verified scroll-away/back). Reduced motion: complete composition immediately (`ready`, opacity 1, dashoffset 0, portrait unclipped).

**QA** (`scratchpad/verify-about-final.js`, 1920×1080 / 1440×900 / 1366×768 / 768×1024 / 390×844 / 360×800 / 683×384 = 200%): one SVG on the page, no overflow, first content below the bar, portrait 4:5 and fully covered, zero text/label/geometry overlaps, reading order 01>02>03, footer cream, no JS errors; tab order bar → founder links → handoff → footer.

## 45. Website 2.0 cutover: the review build becomes production (2026-09-03)

**What happened.** The approved review build (`/mockups/field/*`, sections 34–44) was promoted to the production routes in one commit; the previous templates were retired. Old production = tag `pre-2026-redesign` (`610654a`). New production = `9016a8b` (tag `website-2.0-launch`). Deployed by the existing GitHub Actions workflow; verified on the live domain afterwards (desktop 1366/1440/1920, phone 390/360, tablet 768, 200%% zoom; metadata, OG images, console, links).

**Moves.** `mockups/field/index.njk → index.njk`, `about.njk → about.njk`, `contact.njk → contact.njk`, `public-thinking.njk → public-thinking/index.njk`, `de-public-thinking.njk → de/public-thinking/index.njk`, `pt-item.njk → public-thinking/item.njk`, `de-pt-item.njk → de/public-thinking/item.njk`; layouts `mockup-field/about/contact/pt.njk → home/about/contact/pt.njk` on a new shared `partials/head.njk` (SEO metadata, JSON-LD, hreflang alternates and analytics ported from the old `base.njk`). Internal links lost their `/mockups/field` prefix; `noindex` and "not for publication" titles are gone; pages are back in `collections.all`, so the sitemap lists them.

**Removed.** `src/mockups/` (field review pages incl. `researcher.njk`, and the earlier `/mockups/home/` proposal), `approach.njk` (now the `#approach` chapter), `layouts/base.njk` + the five mockup layouts, partials `nav`, `footer`, `closing-cta`, `ecosystem-diagram(-staged)`, `geometric-field`, `pt-card` (old card), assets `home-mockup.css/js`, `marquee.js`, `pt-rail.js`. All of it is in git history at `pre-2026-redesign`. Kept: `style.css` (archive and article body still render from it), `pt-motifs.njk` (the old `ptMotif()` watermark is still the article hero), `geo-plate.njk`, `node-mark.njk`.

**Server.** `.htaccess` gained 301s: `/mockups/field/(.*) → /$1`, `/mockups/home/ → /`, `/approach/ → /#approach`, `/system/ → /#system`, `/work/ → /#cases`, `/questions/ → /#questions`. `llms.txt` points Approach at the chapter. The SFTP action does not delete remote files, so the old `/approach/index.html` and `/mockups/**` still exist on disk but are never served (redirects win).

**Untracked, deliberately not committed:** `Communitygeeks-Tripo-EMEA-Sprint.pdf`, `_preview_thread.html`, `src/proposals/tripo/` (a client one-pager; it only appears in local builds, never in CI output).

## 46. Typography audit after launch: the closing invitation (2026-09-03, production)

Tester feedback (Darlene, laptop): the homepage's closing "Talk about what's actually happening." ran at hero scale. Measured hierarchy before the fix at 1366 / 1440 / 1920: hero h1 86 / 91 / 96px, chapter h2s 40–58px, closing h2 85 / 89 / 92px. `.close-h2` now uses `clamp(38px,4.4vw,62px)` (60 / 62 / 62px), the same scale as the sub-page h1s, so the closing is the second-loudest moment on the page rather than a second hero. Phones unchanged (43px at 390). No other heading needed correction.

## 47. Contact on phones: the form card is the opening (2026-09-03, production)

Tester feedback (Darlene, phone): the mobile station drew an empty cream bay, cropped by the screen edge, with the form as a separate card below it; it read as broken ("is it not supposed to go in the circle?"). The mobile station (`contact.js`, `drawStation(svg, true)`, viewBox 390×170) now consists of the ring segment and the approach trajectory only; the trajectory descends to the bottom edge of the drawing, which is the top edge of the form card directly beneath (`.dock` margin-top 0), so the form is the opening the vector docks into. The card carries the registration corners (`.dock::before/::after` ≤860px) and the coral `st-edge` bar sits on the seam and lights on success. Checkpoints still advance per valid stage. Verified at 390, 360, 768 and 683×384: seam gap 0, edge bar within the card width, no overflow, stage 1 → 2 lights checkpoint 1. Desktop untouched.
